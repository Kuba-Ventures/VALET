# VALET
*Voice-Activated Local Engineering Terminal — a downloadable, license-gated macOS voice assistant.*

*Last updated: 2026-06-21 ET by kuba-vault*

---

## TL;DR  [rewrite]

VALET (formal name; answers to "Vee") is a local, voice-first macOS assistant with a British-butler persona, an audio-reactive Three.js orb, and a live process panel. The full commercial loop is real and LIVE: buy, sign in at valet-voice.com, download a signed/notarized macOS app, install with no Gatekeeper warning, run. The current arc (sponsor mandate, Jacques Arnoux / Twin Peaks weekly) is the **native, system-wide Mac-control push** — make the agent FAST and intuitive, build on Clicky, deprioritize billing/admin. **This session restructured VALET into a MENU-BAR PRODUCT**, built in four staged steps (`coding-plans/`), with Stages 1–3 done and Stage 4 (the signed DMG capstone) pending. **Stage 1 (#135) + on-device hardening (#136):** VALET is now a menu-bar app (`ActivationPolicy::Accessory`, no Dock icon) with a tray icon/menu, an always-on-top draggable orb popover, and a global held **⌃⌥ chord = push-to-talk from any app** — the tap runs in the MAIN Tauri process (raw core-graphics `CGEventTap`, so macOS Input Monitoring is one stable grant), with a multi-monitor click-through cursor follower and `prevent_exit` so closing the orb keeps it in the tray. **Stage 2 (#137) + file-search quality (#138):** voice-native Raycast — no-LLM search across files (Spotlight `mdfind`, scoped to document folders), home folders, system-settings deep links, and a tiered system-action set (lock/sleep/volume Tier-0; empty-trash/restart/shutdown Tier-1 gated). **Stage 3 (#139, open):** guided walkthroughs (teach, don't do) — given a goal, plan steps, glide the visible cursor to each control WITHOUT clicking, narrate, wait + re-observe until the user does it, then advance ("do it for me" is the only click, gated) — plus menu-bar UI polish (drag-from-anywhere, Settings-from-tray, readable purple PTT hint, black template tray icon). **The key on-device finding:** `tauri dev` can't run this app on macOS (Web-Speech STT is TCC-gated and needs a real `.app` bundle), so testing now uses a **signed debug `.app` bundle** codesigned with the Developer ID so the Input-Monitoring grant persists across rebuilds. Merged to main: #135, #136, #137, #138. Open: #139. **Everything is still dev/debug-bundle only — not in any released DMG.** Stage 4 (fold it all into the signed/notarized DMG + wire Input-Monitoring into onboarding + point `DOWNLOAD_URL` at it) is the outstanding gate before the demo "ships." Still open below it: turn ON fair-use enforcement (#59 mechanism shipped, `FAIR_USE_MODE` still `warn`), device-settings Phase 4, account-login PR A deploy, and Stripe **payouts remain PAUSED** (a deliberate de-prioritization per the mandate).

---

## What it is  [rewrite when value prop evolves]

**The problem:** Talking to LLM chat boxes is slow, and stitching together Calendar, Mail, a browser, terminals and a coding agent to get one thing done is slower. Shipping that as a product also means no end user should have to paste in their own API keys.
**The solution:** A single voice loop on the Mac that routes intent — conversation, system lookup, research, file/app control, ship-to-Cursor — without leaving the orb, with all AI billed centrally behind a license so the download ships with zero vendor secrets.
**The user:** A macOS builder who lives in Claude Code, Cursor and the Apple suite and wants hands-free orchestration — and, post-Phase-2, a paying customer who buys a license and downloads a signed app.
**The value:** Sub-second voice for the simple stuff, full Opus + web tools for the hard stuff, native macOS control with a safety net — and a clean install with no key management.

---

## Status  [rewrite]

- **Phase:** post-MVP iteration — full buy/login/download/install/run loop is live; the **menu-bar product restructure** is the active arc (Stages 1–3 done, Stage 4 the signed-DMG capstone pending), continuing the native-Mac-control push per the sponsor mandate. Stages 1+2 are merged to `main` (#135–#138); Stage 3 + polish is open in #139. Everything is dev/debug-bundle only. Remaining: land #139, then cut a signed build carrying the whole menu-bar product (Stage 4), plus the carried caveats (UC4 focus hardening, live in-app passes) and the deferred config flips (fair-use, payouts).
- **Engagement manager:** Jacques Arnoux (Twin Peaks, weekly) — sponsor mandate: native + FAST + intuitive Mac control, build on Clicky, deprioritize billing/admin
- **Lead:** Finley
- **Cadence:** weekly sponsor sync (Twin Peaks) + continuous per-PR through the supervised factory
- **Next milestone:** **land Stage 3 (#139)** then **Stage 4 — fold the whole menu-bar product into a signed/notarized DMG**, wire the macOS **Input Monitoring** grant (for the ⌃⌥ chord) into onboarding/Permissions alongside Accessibility + Screen Recording, and point `DOWNLOAD_URL` at the new build. Billing/admin (fair-use flip, payouts) deliberately deprioritized. No hard date.
- **Flags:** shipping

---

## Where we are right now  [rewrite]

This session (2026-06-21) restructured VALET into a **menu-bar product** — the form factor the sponsor mandate (Jacques Arnoux, Twin Peaks weekly: native + FAST + intuitive, build on Clicky, deprioritize billing/admin) pointed at, built in four staged steps (`coding-plans/`). Stages 1–3 are done; Stage 4 (the signed-DMG capstone) is the remaining gate.

**Stage 1 — menu-bar shell + ⌃⌥ push-to-talk (#135, merged) + on-device hardening (#136, merged).** VALET is now a menu-bar app: `ActivationPolicy::Accessory` (no Dock icon), a tray icon + menu, and the existing `main` webview repurposed as an always-on-top, frameless, draggable orb popover; `prevent_exit` keeps it in the tray when the orb closes. Activation is a **global held ⌃⌥ chord = push-to-talk from any app** (supersedes the bare-⌥ global PTT — ⌥ collided with Option-typing). The critical on-device correction (#136): the ⌃⌥ tap had to move out of the backend helper and into the **MAIN Tauri process** (`spawn_global_chord`, a raw `core-graphics` `CGEventTap`, not `rdev`) — because macOS Input Monitoring is granted per-executable and the helper's hash churns every build, so the helper-side tap silently got zero events; the retired `global_ptt.py` sidecar tap is gone. The multi-monitor cursor follower now places one click-through overlay per display in the global logical point space. **And the headline finding:** `tauri dev` can't run this app on macOS — the webview auto-starts Web-Speech STT (TCC-gated), and TCC only honors the bundle's `NSSpeechRecognitionUsageDescription` from a real `.app`, so the dev binary is SIGABRT'd ~3s in. On-device testing now uses a **signed debug `.app` bundle** (`tauri build --debug`, codesigned with the Developer ID `ai.valet.desktop` / team QZX7VBLDZT) so the Input-Monitoring grant persists across rebuilds.

**Stage 2 — voice-native Raycast (#137, merged) + file-search quality (#138, merged).** The no-LLM fast path (`detect_action_fast` → action → executor) now covers more than app launch. Three new pure modules mirror `app_index.py`: **`file_index.py`** (Spotlight `mdfind`, scoped to Desktop/Documents/Downloads/iCloud, timeout-capped so a cold query never blocks the hot path), **`settings_index.py`** (`x-apple.systempreferences:` deep-link map + fuzzy match), **`system_actions.py`** (lock/sleep/screensaver/volume Tier-0; empty-trash/restart/shutdown Tier-1, routed through the confirm card + kill switch). Home folders open directly. File-intent is gated so a bare "open my X" stays a project; multiple file hits disambiguate by voice. Latency is recorded via `/api/latency/last`.

**Stage 3 — guided walkthroughs, teach-don't-do (#139, OPEN) + menu-bar UI polish (same PR).** New `walkthrough.py`: given a goal, plan steps (curated seeds for demo reliability + a forced-tool Sonnet fallback), glide the **visible cursor to each control WITHOUT clicking**, narrate, then **wait + re-observe until the user does it** (auto-detect advance with a "next"/"do it for me"/"stop" voice fallback and a runaway hard-cap). The only click path is an explicit "do it for me" → the existing gated `_resolve_and_act`. It reuses the proven seams (perception/resolver/glide/speak/kill) and is unit-tested via injected I/O (12/12 headless). Polish in the same PR: drag the orb from anywhere, Settings opens from the tray menu (the in-orb three-dots is gone), a readable purple "hold ⌃⌥ to talk" hint, and a black orb TEMPLATE menu-bar icon.

**The next step.** Land #139, then **Stage 4 — fold the whole menu-bar product into the signed/notarized DMG**: wire the macOS **Input Monitoring** grant (for the ⌃⌥ chord) into onboarding/Permissions alongside the existing Accessibility + Screen Recording grants, build/sign/notarize, and point `DOWNLOAD_URL` at it. On-device validation of Stages 2–3 still runs through the signed debug bundle.

---

*Foundation (prior sessions, all merged to `main`):* this menu-bar product sits on the voice-native Mac-control foundation (#113–#123: point-and-teach, no-LLM voice console, visible cursor glide + hit-test click, frictionless voice clicks, PTT-primary) and the native-overlay layer (#125–#128 + global PTT: the eng-reviewed cursor follower, the web-click AX fix, and the WS-broadcast global PTT — the latter now superseded by the in-main-process ⌃⌥ chord). The anchoring correction throughout: open-source Clicky teaches/points but doesn't execute; VALET's Universal Control already CLICKS, so **speed is the product**.

**What shipped, in order.** **#113 point-and-teach** — "where is X" speaks the on-screen location (voice-only, structurally input-free): `target_resolver` now carries the element frame, a new `emit_pointer`, and `_resolve_and_point`. **#114 Stage 0 latency harness** — per-turn timing + `GET /api/latency/last` (reused/extended `voice_timing.TurnTimer`). **#116 Stage 1 voice console** — "open X" launches installed apps with NO LLM round-trip via a new `app_index.py` fuzzy matcher (nested `.app` bundles, space-insensitive). **#117 Stage 2 visible cursor glide + hit-test-guarded click** — `move_cursor` in `accessibility_executor.py` (threaded tween, abort-on-physical-mouse, an AX hit-test before the click), wired into `_resolve_and_act`, with an Escape hard-stop. **#118** Escape now kills in-flight voice (not just sleep) via a `_speak_cancel` flag in `_speak_chunks`. **#119** an in-app cursor-takeover banner ("Vee is steering your cursor"). **#120** no confirm panel for voice clicks (raw AX executor, kill-switch still honored) + a faster miss (~5s→~1-2s: `resolve()` runs vision at most once; the "X in app" retry is AX-only via `vision=False`). **#121** an echo-loop fix (post-speech cooldown so Vee stops hearing and replying to itself). **#122 PTT-primary** — push-to-talk is now the **default** activation (hold ⌥/Option to talk), with a toggle for always-listening (wake-word), killing wake-word latency for the focused case. **#123** roadmap + backlog docs.

*(That foundation's then-"next step" — the native-overlay spike — is what this session executed: eng-review #125, cursor follower #126, global PTT in-dev. A Raycast console v2 remains incremental work on top of the #116 voice console.)*

**Must not forget (carried forward).** Everything from this and the last four sessions is **dev/debug-bundle only** — none of it (menu-bar shell + ⌃⌥ chord PTT, voice-native Raycast, walkthroughs, native overlay, web-click fixes, voice console / cursor glide / PTT-primary, account-login + latency, UC0–UC6) is in any released /Applications DMG, so customers have none of it until Stage 4 cuts the signed build; that rebuild is the outstanding gate before the demo "ships." **Input Monitoring is now a first-class onboarding permission to wire** — the ⌃⌥ chord runs in the signed main process (so the grant is stable once the DMG ships and signs), but it still needs to be requested + surfaced in onboarding/Permissions like Accessibility and Screen Recording. **Demo cadence is revised:** test from a signed debug `.app` bundle, not `tauri dev` (the webview's TCC-gated Web-Speech STT SIGABRTs a non-bundle binary). Still standing: branch protection needs "require branches up to date before merging" enabled (the merge train dropped commits twice — #114/#116, restored via #115); an exposed AssemblyAI eval key still needs rotating; the prior-session UC follow-ups (synthetic-input focus hardening, live in-app confirm/STOP/permission passes, UC5 echo tuning, keep-STOP-visible-during-loop); and the deferred billing config (fair-use flip, device-settings Phase 4, account-login PR A deploy, payouts) all stand below.

---

## What's left to pick up  [rewrite — read this first]

This is the exact next-step list, ordered — sponsor mandate (FAST + intuitive Mac control) first, billing/admin deliberately last.

1. **Land Stage 3 (#139).** Guided walkthroughs (`walkthrough.py`, teach-don't-do) + menu-bar UI polish are in the open PR; on-device validation via the signed debug bundle is pending. Backend Python + Tauri config → escalate-tier, human-reviewed merge.
2. **Stage 4 — cut the signed/notarized DMG carrying the whole menu-bar product (top shipping gate).** Fold in the menu-bar shell + ⌃⌥ chord PTT + voice-native Raycast + walkthroughs + the native overlay + the voice-console / cursor-glide / PTT foundation + account login + **UC1–UC3**, gated behind the Accessibility + Screen Recording **+ Input Monitoring** grants in onboarding. Hold UC4 (loop), UC6 terminal, and UC5 (barge-in) for a build *after* the focus hardening + live in-app passes below. Build/sign/notarize, upload, and point `DOWNLOAD_URL` at it.
3. **Wire Input Monitoring into onboarding/Permissions.** The ⌃⌥ chord runs in the signed main process (so the grant is stable once shipped), but onboarding + the Console Permissions section must request + surface it like Accessibility / Screen Recording. Part of Stage 4.
4. **Enable branch protection: "require branches up to date before merging".** TWO squash-merges silently dropped commits in an earlier session (#114 dropped #113's `server.py`; #116's nested-bundle fix dropped) and had to be restored (#115). Turn this on so the merge train can't drop a parent's commits again.
5. **Rotate the exposed AssemblyAI eval key** (from the Clicky benchmarking transcript). Likely already tracked; confirm rotated.
6. **Deploy account-login PR A (#97) to Vercel.** The app side (#98–#102) is on main, but `/api/account/login` forwards to the proxy's `/api/account/app-login` (PR A) — account login only works end-to-end once `product-site` is redeployed. Until then the manual license-key fallback is the path.
7. **Synthetic-input target-focus hardening (UC4).** Keystrokes go to whatever app holds keyboard focus (a terminal stole them in CLI tests). Needs robust app-focusing before typing. Blocks shipping UC4 in a signed build.
8. **Live in-app passes owed (in a running/signed build).** UC4 confirm-card/STOP click-through of the observe→act loop; the Screen-Recording permission-UI grant/revoke→red; UC5 barge-in echo tuning (needs real speech, mic/speaker-dependent). Plus the UI fix: the STOP button is hidden when idle (`main.ts`) — the UC4 loop must set the "active" state so STOP stays visible throughout the loop.
9. **Raycast console v3 (incremental).** Stage 2 (#137/#138) shipped the core — no-LLM file / settings deep-link / system-action search. Remaining nice-to-haves: calculator, snippets, clipboard history, window management.
10. **Turn ON fair-use enforcement** (deprioritized per the sponsor mandate, but still the top spend risk; mechanism shipped in #59). In Vercel set `FAIR_USE_MODE=throttle` (or `block`) and the per-plan envs `FAIR_USE_USD_PRO` / `FAIR_USE_USD_ULTRA`. Until set it stays `warn`.
11. **Finish device-settings Phase 4** (bidirectional "manage VALET from the browser"). Phases 1–3 landed (#66/#68/#69). **Remaining Phase 4:** conflict resolution (web wins) + decide poll cadence.
12. **/admin usage analytics dashboard (backlog).** `coding-plans/backlog.md` — needs a "VALET-usage-vs-OS-surveillance" privacy decision + a local→Supabase telemetry pipeline before building.
13. **Fix mic-green permission detection (backend).** `/api/permissions/status` returns null for Microphone, so the indicator never turns green even when granted — affects both onboarding and the Console Permissions section. Needs a real mic/speech TCC authorization check via the bundled pyobjc.
14. **Design decision:** should "open my calendar" / "open email" launch the app or show the data? Today they route to the calendar/mail READ lookups. Open design call.
15. **Stripe payouts** — client's responsibility: add a bank account to resume payouts. Charges work; funds just can't pay out yet.

---

## What's built  [rewrite]

### Stage A — Proxy spine (LIVE in prod)
- License-gated AI/TTS proxy in `product-site/` (Next.js on Vercel, https://jarvis-y.vercel.app). Routes: `app/api/proxy/{completion,research,tts,usage}` plus a native Anthropic-dialect `app/api/proxy/v1/messages`.
- Backed by `product-site/lib/proxy/{auth,pricing,usage,langfuse,anthropic,fish}.ts`. Anthropic + Fish keys live server-side; the downloadable app ships with no vendor secrets.
- Every call validated against the Supabase `licenses` table; usage metered per license (requests, tokens, est. cost) against a **per-plan** fair-use allowance (`FAIR_USE_USD_PRO` / `FAIR_USE_USD_ULTRA`, fallback `FAIR_USE_MONTHLY_USD`; #59). Behavior is still **soft-warn** because `FAIR_USE_MODE` defaults to `warn` — it never blocks until the owner sets `FAIR_USE_MODE=throttle`/`block`.
- Traced to Langfuse with the license as user id, payloads scrubbed by default.
- `product-site/supabase/migration_usage.sql` adds the `license_usage` table + atomic `record_usage()` RPC (30-day rolling, lazy reset). Zero new npm deps.

### Stage B — App repoint + license gate (LIVE)
- `server.py` routes all AI + TTS through the proxy: Anthropic SDK `base_url` → `{PROXY_BASE_URL}/api/proxy`, license in `X-License-Key` (`server.py:2930`); `synthesize_speech` → `/api/proxy/tts` (`server.py:2460`).
- `licensing.py` validates against `/api/license/validate` with a 7-day offline grace window (state in `data/license_state.json`); the assistant loop refuses when not entitled.
- App stores only `LICENSE_KEY` + `PROXY_BASE_URL`; in-app API-key entry removed. Settings now expose License Key + Proxy URL + a `test-license` action.
- Dev fallback (no `LICENSE_KEY` → direct keys) preserved for the internal repo only.
- Also extracted from `server.py`: `task_manager.py`, `voice_text.py`, `project_scanner.py`. The tightly-coupled voice-dispatch core (`voice_handler` + `_execute_*` handlers + lookups) was deliberately left in `server.py`.

### Stage C — Control layer (MERGED)
- Portable `action_executor.py` — `ActionExecutor` ABC + `ActionResult` + `Capability` enum (open app/path, read/write/move/delete file, list folder, run app command, send keystroke, navigate, run script).
- `applescript_executor.py` — macOS backend. Detects app scriptability via the bundle's `.sdef`/Info.plist without launching; non-scriptable apps return a structured, logged `not_supported`. Deletes go to Trash. Portable so a future Windows/Linux (or Accessibility/vision) backend is a swap.

### Stage D — Risk-tiered safety (MERGED)
- `safety.py` — `classify()` → Tier 0 (auto) / Tier 1 (confirm); `KillSwitch`; `ConfirmationManager`. Tier 0 = reads/lists/opens/navigate; Tier 1 = delete/move/overwrite, drive apps, keystrokes, scripts, bulk ops, protected paths. New-file write is Tier 0; overwrite is Tier 1.
- `safe_executor.py` — `SafeExecutor` decorator over the Stage C executor (confirm-first, Trash-safe, kill-aware).
- `server.py` routes `[ACTION:DELETE_FILE]` + raw `[ACTION:APPLESCRIPT]` through it.
- Frontend `confirmCard.{ts,css}` (holographic Allow/Deny card) + an always-available STOP kill-switch button.
- REST `/api/safety/{kill, kill/reset, status}` + WS confirm/kill round-trip.

### Stage E — Voices + cut self-editing (MERGED, PR #17)
- Two selectable British voices (Male/Female), persona unchanged — `VALET_VOICE` + `VALET_VOICE_{MALE,FEMALE}_ID`; `_active_voice_id()` reads live and is sent as the Fish `reference_id` (applies on the next reply, no restart). Settings has a Male/Female toggle. Both voice IDs set (male `612b878b…`, female `b347db033a6549378b48d00acb0d06cd`).
- Self-editing disabled in shipped builds — all `self_mod` access routes through `_load_self_mod()`, which returns `None` when shipped (`VALET_SHIPPED` set or no `.git`), so `self_mod.py` is excludable. Dev-repo behavior unchanged.

### Stage F — Packaging, signing, distribution (DONE, LIVE)
- **Signed + notarized macOS app exists and is live.** `packaging/build-macos.sh` runs PyInstaller `--clean` to bundle the FastAPI backend, Tauri builds + signs the `.app` (Developer ID Application, Team `QZX7VBLDZT`), then `xcrun notarytool submit --keychain-profile valet-notary --wait` notarizes and staples. Installs with no Gatekeeper warning.
- **Distribution:** the DMG is a public GitHub release on `Kuba-Ventures/valet-downloads` (tag `v0.1.0`, asset `VALET_0.1.0_aarch64.dmg`). Vercel env `DOWNLOAD_URL` points at it; `product-site/app/api/download/route.ts` validates the license then redirects to that URL (placeholder fallback only when the env var is unset).
- **First-run onboarding wizard** (`frontend/src/onboarding.{ts,css}`): welcome → license activation → permissions (mic, computer control, accessibility, full-disk) → voice → profile (name / DOB / location) → connections. Non-blocking (steps are skippable). Re-runs on every new build, and on every fresh install via the `.app` creation-time stamp (so re-downloading the same build re-triggers it).
- **Build reliability:** `--clean` (PR #42) prevents shipping a stale cached `server.py`/frontend; the watchdog + `:8340` free-before-bind fix (PR #43) makes relaunch after a permission toggle reliable instead of hanging.

### Self-service accounts + LIVE billing (THIS SESSION)
- **Stripe is LIVE.** Flipped from sandbox to live under a new account "Twin Peaks Labs": live secret/publishable keys, a live webhook at `https://www.valet-voice.com/api/stripe/webhook` (checkout.session.completed, customer.subscription.created/updated/deleted), recreated Pro/Ultra products with new live price IDs in Vercel (`STRIPE_PRICE_ID_PRO`/`STRIPE_PRICE_ID_ULTRA`), Customer Portal config saved in live mode. Verified live via curl — both tiers create `cs_live_` sessions; a real trial purchase issued `PRODUCT-HGE2-H65Z-78W6-Z7DC-RERS`. **Payouts PAUSED** until a bank account is added (activation incomplete).
- **Email + password auth** via Supabase Auth + `@supabase/ssr` (PRs #52, #53, #55). New `/account` section: login, signup, password reset, update-password, `/auth/callback` (code exchange). Email verification enforced (no session until confirmed; the license works in the app regardless). `middleware.ts` refreshes the session cookie and gates `/account/*`. Auth UI in `components/account/*` (`AuthForm`, `ResetForm`, `UpdatePasswordForm`, `PasswordInput` with eye-icon show/hide + confirm field, `SignOutButton`, `ManageBillingButton`, `ClaimLicenseForm`); Supabase clients in `lib/auth/{server,client}.ts`.
- **Account ↔ license linking:** `licenses` gained `customer_email` (from Stripe in `lib/license.ts`) and `user_id`. New accounts auto-claim a license by matching the buyer email on dashboard load; a manual "link key" form (`/api/account/claim`) covers a mismatched address. Data layer in `lib/account.ts` (`linkLicensesByEmail`, `claimLicenseByKey`, `getAccountLicenses`), all scoped to the verified session user via the service-role key.
- **Customer dashboard** (`app/account/page.tsx`): license key (copy), plan/tier, status, renew/trial date, per-period usage (voice requests, tokens, est. cost vs fair-use allowance — reuses `license_usage`), download link, **Manage billing** → Stripe Billing Portal (`/api/stripe/portal`). Success page now points buyers at `/account`; Nav gained an Account link.
- **Owner admin view** (`app/account/admin`, PR #54): owner-only, read-only. Summary cards (active, on-trial, est. MRR, period spend) + a subscriber table (email, tier, status, renew/trial, joined, requests, est. cost, claimed/unclaimed). `lib/admin.ts`: `isAdmin(email)` checks the verified session email against `ADMIN_EMAILS`; `getAllAccounts()` reads all licenses + usage behind that gate. The "Admin" link shows only to allow-listed emails. No per-customer admin actions yet.
- **Desktop → web sync, web side** (PR #56, merged): `account_sync` table (`migration_sync.sql` — `profile`/`stats`/`connections` JSONB + `app_version`, one row per license, on-delete cascade, RLS-on no public policy). Authed ingest `POST /api/proxy/sync` (X-License-Key via `authorizeLicense`) with a strict allow-list sanitizer (known fields only, types coerced, extras dropped, no message content). Dashboard renders Profile, "Speech & activity" (tasks, success rate, avg time, top requests) and Connected apps; degrades to a "waiting for your app" state before first sync. `lib/account.ts getLatestAccountSync`.
- **Desktop → web sync, app side** (PR #57, MERGED): `server.py` wires the previously-test-only `SuccessTracker` (`tracking.py`) into the live loop at two choke points — `_track_usage(action)` at chat dispatch (top requests) and `_track_task(type, success, duration)` + connection detection in the shared `_lookup_and_report` wrapper (success rate, avg duration, calendar/mail/notes "seen working" flags). No-op-safe; the voice hot path is untouched on tracker failure. Tracker DB opens in the writable data dir via a new `valet_data_dir()` helper. A telemetry-gated background loop (`_account_sync_loop`, 20s after start then every 15 min, gated on LICENSE_KEY present AND VALET_TELEMETRY not opted out) pushes `_gather_sync_snapshot()` (onboarding profile env + tracker aggregates + connection flags + app version; skips the "sir" USER_NAME placeholder; no message content) to `/api/proxy/sync`.
- **Migrations run in prod:** `supabase/migration_accounts.sql` (adds `customer_email`, `user_id`, indexes) and `supabase/migration_sync.sql` have both been applied to production Supabase. `supabase/migration_device_settings.sql` (for #66) is **not yet applied** — run it when #66 lands.

### Hardening + per-plan limits (LATEST — since the account portal / live-Stripe update)
- **Per-plan fair-use allowances** (PR #59): the fair-use ceiling is now per plan — `FAIR_USE_USD_PRO` / `FAIR_USE_USD_ULTRA` with `FAIR_USE_MONTHLY_USD` as fallback — threaded through enforcement (`lib/proxy/{auth,usage,anthropic,fish}.ts`) and the displayed cap. This is the mechanism that turns the soft cap into a real per-license limit. It does **not** enforce yet: `FAIR_USE_MODE` still defaults to `warn`. To close the shared-key risk the owner must set `FAIR_USE_MODE=throttle` (or `block`) + the per-plan allowance envs in Vercel.
- **Usage shown as a percentage** (PR #60): the customer dashboard shows usage as a % only — no dollar spend or limit exposed. A server component computes it so the raw numbers never reach the browser. Owner admin still shows $ / MRR.
- **Auth-aware nav** (PR #61): signed-in users see a blue "Account" button; signed-out see "Log in" + "Start free trial". Client-side session detection so marketing pages stay static.
- **No duplicate backend Dock icon** (PR #62): the PyInstaller backend imports AppKit (pyobjc) which gave it a Dock icon; now set faceless via `NSApplicationActivationPolicyProhibited`. Good hygiene — but NOT the actual cause of the duplicate-Dock issue the user saw (that was stale mounted DMG volumes + a trashed copy polluting Launch Services).
- **Three-dot opens Settings** (PR #63): the app's three-dot button opens Settings directly; removed the dev-only "Restart Server" / "Fix Yourself" dropdown items.
- **Settings split into User + Console** (PR #64): "Computer Settings" → "Console Settings". Console gained a **Permissions** section (Microphone via native getUserMedia prompt, Automation, Full Disk Access — each enable + Re-check, reusing `/api/permissions/{status,open}`) and a **Connectors** section (Google moved here). User tab keeps personal info (name, honorific, calendar accounts, DOB, location, bio).
- **Open-Notes app-launch + device-settings (MERGED):** **#65** — `_looks_like_app()` strips trailing locational suffixes ("on my computer/mac/...", " app") so "open Notes on my computer" launches the app rather than the project resolver. **#66** — device-settings Phase 1 (store + endpoints: `device_settings` table + `lib/device-settings.ts` + `GET /api/proxy/device-settings` app-reads + `GET/POST /api/account/device-settings` web-reads/writes; deploy needs `supabase/migration_device_settings.sql`). Phases 2–3 also landed: **#68** editable Device Settings card on `/account`, **#69** app applies web device-settings at launch + removes Proxy URL + Voice ID from the app. Phase 4 (conflict resolution / poll cadence) remains open.
- **Transactional email LIVE:** Resend domain `valet-voice.com` verified (DKIM/SPF/MX); Vercel `RESEND_API_KEY` + `EMAIL_FROM` set (license-key email sends on purchase); Supabase custom SMTP wired to Resend (`smtp.resend.com:465`, user `resend`) so signup-confirmation + password-reset emails deliver. www `/auth/callback` redirect added in Supabase; `NEXT_PUBLIC_SUPABASE_URL` `/rest/v1/` suffix removed. Account creation verified end-to-end on a clean slate.

### Voice-native Mac control — fast launch + visible cursor + PTT-primary (MERGED to main — #113–#123)
A speed/intuitiveness push (sponsor mandate: FAST + intuitive, benchmark Hey Clicky, build on Clicky). Scoped via `/office-hours` (design doc under `~/.gstack/projects/Kuba-Ventures-VALET/`), built in stages (`coding-plans/voice-mac-control-build-plan.md`). All dev-server-only — not in any signed build.
- **Point-and-teach (#113):** "where is X" speaks the on-screen location and points, without moving or clicking anything (structurally input-free — a locate-only slice of UC3). `target_resolver` now carries the resolved element's frame; new `emit_pointer` event; new `_resolve_and_point` in `server.py`. (Restored after the #114 squash-merge reverted its `server.py` changes — see #115.)
- **Stage 0 latency harness (#114):** per-turn timing for Mac-control actions end-to-end + `GET /api/latency/last`, reusing/extending `voice_timing.TurnTimer`. Measure-first before optimizing the action path.
- **Stage 1 voice console (#116):** "open X" launches any installed app with **NO LLM round-trip** — a new `app_index.py` fuzzy matcher walks the install locations (handles nested `.app` bundles and is space-insensitive). The Raycast-style instant-launch primitive. (Its nested-bundle fix was dropped by a squash-merge and restored.)
- **Stage 2 visible cursor glide + hit-test-guarded click (#117):** `move_cursor` in `accessibility_executor.py` — a threaded tween that visibly glides the cursor to the target, aborts the moment a physical mouse moves, and runs an **AX hit-test before clicking** (only clicks if the element is actually under the cursor). Wired into `_resolve_and_act`. Includes an **Escape hard-stop**.
- **Escape kills in-flight voice (#118):** Escape now cancels active speech, not just sleep — a `_speak_cancel` flag checked in `_speak_chunks`.
- **Cursor-takeover indicator (#119):** an in-app banner ("Vee is steering your cursor") so the takeover is never silent.
- **Frictionless voice clicks (#120):** voice clicks no longer raise a confirm panel — they run through the raw AX executor (the **kill-switch is still honored**). Misses got faster (~5s → ~1-2s): `resolve()` runs the vision pass **at most once**, and the "X in app" retry is AX-only (`vision=False`).
- **Echo-loop fix (#121):** a post-speech cooldown so Vee stops hearing its own TTS and replying to itself.
- **PTT-primary (#122):** push-to-talk is now the **default** activation — hold ⌥/Option to talk — with a toggle for always-listening (the wake-word path). Kills wake-word latency for the focused case. (Supersedes the #92 opt-in-off default.)
- **Roadmap + backlog docs (#123):** captured the remaining Clicky/Raycast roadmap and the dashboard backlog (`coding-plans/`).
- **Strategic correction recorded:** open-source Clicky teaches/points but doesn't execute; VALET's Universal Control already CLICKS, so the differentiator is **speed**, not feature parity.

### Menu-bar product restructure (THIS SESSION — Stages 1–2 on main #135–#138, Stage 3 open #139)
The front-door restructure: VALET is now a menu-bar app, not a windowed one. Built in four staged steps (`coding-plans/`), all dev/debug-bundle only (Stage 4 cuts the signed DMG).

**Stage 1 — menu-bar shell + ⌃⌥ chord PTT (#135) + on-device hardening (#136):**
- **Menu-bar app:** `ActivationPolicy::Accessory` (no Dock icon) + a tray icon and menu; the existing `main` webview repurposed as an always-on-top, frameless, draggable orb popover. `RunEvent::ExitRequested → prevent_exit()` keeps the app alive in the tray when the orb closes; CloseRequested hides (not closes) the orb. Tray-build is non-fatal and uses an embedded `icons/32x32.png` (`default_window_icon()` is None in dev → panic, fixed).
- **Global ⌃⌥ chord = hold-to-talk from any app** (`spawn_global_chord` in `src-tauri/src/main.rs`): a raw `core-graphics` `CGEventTap` in the MAIN Tauri process (NOT `rdev`, which resolves keycodes via main-thread-only HIToolbox and aborts off-main), driving the frontend via a `window.__valetChord` eval hook. Supersedes the bare-⌥ global PTT (⌥ collided with Option-typing) and the retired backend `global_ptt.py` sidecar tap. Cancel-on-other-key: a real ⌃⌥-letter shortcut mid-hold cancels the turn and discards captured audio so app shortcuts still work.
- **Why the tap moved into the main process (#136):** macOS Input Monitoring is granted per-executable and the backend helper's cdhash churns every build → the helper-side tap got zero events. In the signed main process the grant is stable. The dev build must be **codesigned with the Developer ID** (`ai.valet.desktop` / team QZX7VBLDZT) so the grant persists across rebuilds; the shipped DMG already signs.
- **Multi-monitor cursor follower:** one click-through overlay per display, placed and hit-tested entirely in the global LOGICAL point space (the only space macOS keeps consistent across mixed-DPI displays).
- **Demo cadence finding:** `tauri dev` can't run this app on macOS — the webview auto-starts TCC-gated Web-Speech STT, which TCC only honors from a real `.app` bundle's Info.plist, so a loose dev binary is SIGABRT'd ~3s in. Testing uses a **signed debug `.app` bundle** (`tauri build --debug`).
- Orb drag is a visible top header (full-window drag ate the AudioContext-unlock click → no TTS); needed `capabilities/orb-drag.json` granting `core:window:allow-start-dragging` to the `localhost:8340` origin. Headless gates green (pytest 10/10, tsc+vite, `cargo check`); on-device validated via the signed debug bundle.

**Stage 2 — voice-native Raycast: no-LLM file / settings / system-action search (#137) + file-search quality (#138):**
- Broadened the no-LLM fast path (`detect_action_fast` → action dict → executor) beyond app launch via three new pure, unit-tested modules mirroring `app_index.py`:
  - **`file_index.py`** — Spotlight `mdfind`, scoped to Desktop / Documents / Downloads / iCloud, timeout-capped so a cold query never blocks the hot path; pure `rank_hits` / `detect_kind`. File-intent is gated (a browse verb, a kind word, or "file"/"document") so a bare "open my X" stays a project; multiple hits disambiguate by voice ("the second one") via the ordinal parser, a clear winner (score gap ≥25) opens directly.
  - **`settings_index.py`** — `x-apple.systempreferences:` deep-link map + fuzzy `match_setting`. Home folders open directly.
  - **`system_actions.py`** — list-form commands tagged with a safety tier: lock / sleep / screensaver / volume are Tier 0; empty-trash / restart / shut down are Tier 1, routed through the existing confirm card + kill switch (as a background task so the confirm reply never parks the WS loop).
- Dispatch wrapped in `_timed_action` so `/api/latency/last` records sub-second, no-LLM marks. Headless green (3 module suites + `detect_action_fast` routing 8/8; `app_index` 11/11 unregressed).

**Stage 3 — guided walkthroughs (teach, don't do) + menu-bar UI polish (#139, OPEN):**
- **`walkthrough.py`** — given a goal ("show me how to turn on Bluetooth"), VALET plans steps, glides the **visible cursor to each control WITHOUT clicking**, narrates, then **waits + re-observes until the user does it** and advances. Reuses the proven seams (no rebuild): `perception.build_observation` (UC2), `target_resolver.resolve` (UC3), `accessibility_executor.glide_to_target` + `emit_cursor_control` (the point-and-teach gesture — glides, never clicks), `process_bus`/`emit_step` + `_speak`, and `_track_uc`/barge-in/`kill_switch` for cancellation. Authoring = curated seeds (Bluetooth/Wi-Fi/FileVault, demo-reliable) + a forced-tool Sonnet JSON fallback. Advance = pure `step_done(before, after, step)` heuristic auto-advances, with a "next" / "do it for me" / "stop" voice fallback on stall and a runaway hard-cap (4×timeout → `stalled`). **Teach-don't-do:** the loop only points; the sole click path is an explicit "do it for me" → the existing **gated** `_resolve_and_act`. Unit-tested via injected I/O (12/12 headless; uc3/uc4/stage2 unregressed). On-device validation via the signed bundle pending.
- **UI polish (same PR):** drag the orb from anywhere; Settings opens from the tray menu (in-orb three-dots dropped); a readable purple "hold ⌃⌥ to talk" hint; a black orb TEMPLATE menu-bar icon (was a blue-on-black app icon).

### Native overlay + global PTT + web-click resolution (#125–#128 + follow-ups on main)
The native, system-wide layer on top of the voice foundation. Eng-review-gated (#125), macOS-private-API-dependent, all dev/debug-bundle only.
- **Native overlay architecture, locked (#125):** `/plan-eng-review` settled two calls — (1) render the cursor dot as a CSS transform inside a fullscreen transparent click-through overlay, not per-frame window moves (which stutter) and not a Swift sidecar; (2) global PTT via a chord through `tauri-plugin-global-shortcut` (review caught that bare ⌥ alone isn't registerable). Coord space = global points, Retina/multi-monitor-aware. Flagged the one critical gap: click-through must be verified or the overlay eats every click. Stage runs only in `tauri dev`, not the vite server.
- **Native cursor follower, first slice (#126):** a fullscreen transparent, decorations-off, always-on-top, skip-taskbar, no-shadow, click-through Tauri window (`src-tauri/loading/overlay.html`) holding a CSS dot; `spawn_cursor_overlay()` in `src-tauri/src/main.rs` builds it, sets `set_ignore_cursor_events(true)` for click-through, then runs a ~60fps Rust poll loop that reads the global cursor (physical px → logical pt via `/scale`) and moves the dot via `window.eval()` (Rust→webview — no Tauri JS API / capabilities needed; only the dot's CSS transform changes, never the window, so motion stays GPU-smooth). Enabled Tauri `macOSPrivateApi` (`tauri.conf.json`, required for transparent windows) + the `macos-private-api` Cargo feature (`Cargo.toml`). `cargo check` passes against real Tauri v2; render/smoothness/click-through is the manual runtime check. **First slice only: dot always visible, primary monitor only.** Next slices: show-only-while-steering (consume `cursor_control` events), global ⌥-Space PTT, multi-monitor.
- **PTT-silence regression fix (#127):** #118 (`if (isSleeping) return;` in the audio handler for instant Escape silence) + #122 (PTT-primary default with `isSleeping=true`) interacted so that in everyday PTT mode Vee executed commands but never spoke — every audio frame dropped. Introduced a dedicated `muted` flag (hard-stop only, distinct from `isSleeping` which in PTT mode means "wake mic off" but Vee must still speak); the audio handler gates on `muted`, Escape sets it, onWake/onCommand clear it. `updateStatus` now blanks the status only when `isSleeping AND idle`, so PTT-hold shows "listening…" and PTT-at-rest shows the "press ⌥" hint. Frontend-only.
- **Web-click accessibility tree fix (#128):** Chromium (Chrome/Brave/Electron) only builds its full AX tree when an assistive tech asks via `AXManualAccessibility` / `AXEnhancedUserInterface`; `observe_ui` never set them, so web pages were invisible to AX (only the toolbar showed) and "click <web thing>" fell back to imprecise vision. `observe_ui` now sets both attributes on the app element before enumerating (one-time per pid, cached in `_a11y_enabled`, with a ~350ms build wait on first touch only; repeat observes skip the wait — no per-click latency). Idempotent, harmless on native apps. 28/28 UC tests pass. Backend-only.
- **Web-click element-COVERAGE follow-up (now merged):** the resolver was missing GitHub's "All pull requests" nav icon and the profile avatar because they sit past the old limits. `target_resolver._elements_block` default 80→200 (the model only ever saw the first 80 interactive elements; rich pages put nav controls past #80, so they were never offered to the picker); `accessibility_executor._MAX_ELEMENTS` 250→400; first-enable a11y build delay 0.35s→0.6s (big pages weren't done building when the first click fired).
- **GLOBAL push-to-talk via a backend CGEventTap (`global_ptt.py`) — SUPERSEDED by the in-main-process ⌃⌥ chord.** The original design ran a Quartz `CGEventTap` on a backend daemon thread broadcasting `{"type":"ptt"}` over the WS so hold-⌥ worked in any app. On-device (#136) it silently received zero key events: macOS Input Monitoring is granted per-executable and the backend helper's cdhash churns every build. The tap was moved into the Tauri MAIN process (`spawn_global_chord`, raw `core-graphics`, driving the frontend via `window.__valetChord`), and the helper-side `global_ptt.py` sidecar tap was retired. The generic `broadcast(message)` added to `ProcessEventBus` (with `emit()` delegating to it) survives as the control-frame fan-out primitive. See the menu-bar subsection above for the live design.

### Account login in the app — provision license + profile in one step (MERGED to main — #97–#102)
Replaces manual license-key paste: a new user logs in once with email + password and the app provisions everything.
- **Product-site backend (PR A, #97):** `getSupabaseAnon()` (anon-key client so server-side `signInWithPassword` actually validates the password — the service-role admin client bypasses auth) and `loginAndProvision(email, password)` → sign in → reuse `getAccountLicenses` (prefer entitled) + `getLatestAccountSync().profile`, typed result distinguishing bad-creds / unconfirmed-email / no-license. New `POST /api/account/app-login` → `{license_key, has_license, status, plan, profile}` and `GET /api/proxy/profile` (X-License-Key) + `getProfileByLicenseKey()` for launch-time re-pull (mirrors device-settings). Design in `docs/account_login_design.md`. **Needs a Vercel redeploy to take effect.**
- **App side (PR B, #98):** a "Log in to account" section under User Settings (email + password) → `POST /api/account/login`, which forwards creds to the proxy's app-login and writes the returned `LICENSE_KEY` + profile (name→USER_NAME, honorific→HONORIFIC, DOB→DATE_OF_BIRTH, location→HOMETOWN_CITY, work/personal email) to `~/Library/Application Support/VALET/.env`. Password forwarded for verification only — never stored or logged. Launch-time `_fetch_and_apply_profile()` (GET `/api/proxy/profile`, **fill-empty-only** so it never clobbers local edits) auto-populates later installs.
- **In-session license activation, no relaunch (PR #102):** the critical fix. After onboarding login the app said "API key not configured" and couldn't speak — the session started with an empty `.env` (no proxy LLM client, empty `LICENSE_KEY` global) and login wrote the key but never rebuilt the running clients. `_reinit_after_license()` now rebuilds the proxy LLM client + updates the `LICENSE_KEY` global (which `synthesize_speech` reads for its `X-License-Key` header) right after a successful login, so LLM + TTS work immediately. **`needs_relaunch` is now always false** (supersedes the #98 "Restart VALET to activate your license" behavior). Also removed the informational onboarding Connections step and the in-app Contacts editor (replaced with a "manage account & contacts in your browser" link; Apple-Contacts resolution still works — a web contacts editor is the follow-up).
- **Onboarding + Settings UX (#99, #100, #101):** #100 added account login to the onboarding **Activate** step (login leads; license-key paste kept below as fallback; the later "About you" step pre-fills from the provisioned profile). #99 styled the login fields (the `[type=email]` input was rendering white/narrow — added it to the settings CSS selector + a `-webkit-autofill` dark-repaint) and collapsed the Account section to a compact "Signed in as <email> · <plan>" state with a Sign out button (new `ACCOUNT_EMAIL` .env key, written on login / cleared on sign out). #101 fixed a **setup-mode stacking bug** that kept asking users to log in after onboarding (startup ran both `checkFirstTimeSetup` and `maybeShowOnboarding` — Settings opened in setup mode behind the wizard; now the wizard returns whether it showed and `checkFirstTimeSetup` only runs when it didn't, plus `exitSetupMode()` fires once licensed) and surfaced Name · Tier · License # in the signed-in view (backend persists `ACCOUNT_PLAN`; `/api/settings/preferences` returns `account_plan` + `license_key`). Eye-icon show/hide on both login forms.
- **Verification:** `py_compile` + `tsc`/`vite` clean across all five PRs; CI + voice suites green. **Not yet in any signed build, and PR A not yet deployed to Vercel** — so account login does not function end-to-end in production until both happen.

### Perceived-latency track (MERGED to main — #90–#96)
A measure-first pass at the voice turn's perceived latency, plus push-to-talk.
- **Instrument first (#90):** `voice_timing.TurnTimer` marks t0 end-of-speech → t1 request-to-model → t2 first model token → t3 first audio, threaded through `generate_response` + `_speak_chunks` behind optional `timer=` params (no behavior change; always logged, mirrored to the Process Panel only when `VALET_VOICE_TIMING` set). Baseline (Haiku 4.5 + prod TTS proxy): model t1→t2 ≈ 1.1s, first-sentence TTS t2→t3 ≈ 1.2s, server-side t1→t3 ≈ 2.3s.
- **Speak on the first sentence (#91):** the conversational reply now **streams** (`client.messages.stream`) and TTS starts on each complete sentence as it lands. A pure `SpeakableStreamer` (`voice_stream.py`) emits complete sentences only up to the first `[ACTION` marker (action-tag text is never voiced; no double-speak). Removes a median ~178ms of model-side wait before first audio on 2-sentence replies. Server-side mid-stream cancel explicitly deferred.
- **Fish latency knob (#93):** the proxy (`product-site/lib/proxy/fish.ts`) was hardcoding the body and stripping Fish's `latency`/`model` levers; now forwards a validated `latency` (default "normal") + optional `model` header, env-tunable via `FISH_LATENCY`/`FISH_MODEL`. Default unchanged — measured delta on VALET's short replies is within network noise and the app buffers the whole clip. Bigger levers (true streaming playback, the proxy hop) documented as follow-ups.
- **Pre-warm + keep-alive the proxy connection (#94):** `synthesize_speech` was creating a fresh `httpx.AsyncClient` per call, paying a full TLS handshake every TTS request (~139ms median measured). Now a shared keep-alive client (`_get_http_client`, pooled, 90s keepalive) + a best-effort startup `_prewarm` task (warms the proxy TLS + Anthropic SDK connection + the static prompt cache via one `max_tokens=1` request; `VALET_PREWARM=0` to disable) + an extracted byte-identical `_static_system()` so the first real turn gets a cache READ. ~139ms off every TTS call after the first.
- **Push-to-talk (#92, #96):** an opt-in, frontend-only hold-to-talk path (`pushToTalk.ts`, default off, configurable key in localStorage) that sidesteps wake-word latency and the "hey vee"→"av" misfire class; when disabled the wake-word path is byte-for-byte unchanged. #96 adds a "press <key> to talk" on-screen hint when PTT is on.
- **STT-provider investigation (#95, no code):** `docs/stt_provider_investigation.md` — recommend **staying on Web Speech** and measuring finalization latency on the prod WebView (Apple/WKWebView, not Chrome/Google as in dev) before paying for a streaming provider; AssemblyAI Universal-Streaming (~$0.15/hr) is the candidate if PTT + instrumentation still show ≳500ms finalization with hurting accuracy.

### App correctness pass + Google integration (MERGED to main — #75–#77)
- **Contacts system** (981e70e): `memory.py` `contacts` table + add/find/list/delete (case-insensitive, unambiguous first-name/prefix match). New `contacts_access.py` reads Apple Contacts via `CNContactStore` as a fallback. New **Contacts** macOS permission (entitlement `com.apple.security.personal-information.contacts` + `NSContactsUsageDescription` + `collect_all("Contacts")` in `packaging/valet.spec` + a Contacts row in the Permissions panel with inline grant via `/api/permissions/trigger`). `DRAFT_EMAIL` resolves a bare-name recipient via the profile store then Apple Contacts and **asks rather than guesses** when unknown (previously fabricated `Nick@sbsrollover.com` — wrong domain). New `SAVE_CONTACT` action (`"name ||| email"`); saved contacts injected into the system prompt with an explicit rule forbidding invented addresses. New `/api/contacts` CRUD + Settings → Console → Contacts (list/add/remove). `pyobjc-framework-Contacts` added to `requirements.txt`.
- **Persistence bug fix** (bundled in 981e70e): `memory.py`'s `valet.db` resolved to a `__file__`-relative path = the read-only per-launch PyInstaller temp dir in the packaged app, so bio/tasks/contacts **never persisted across launches in the shipped app**. Now resolves to a writable dir (`~/Library/Application Support/VALET/valet.db` when shipped, via `valet_data_dir()`). Verified it persists in App Support.
- **Google OAuth wired so Gmail + Google Calendar actually work** (691b613): the endpoints/Settings UI/connect flow existed but the FROZEN app couldn't connect — `google_credentials.json` wasn't bundled and tokens wrote to the ephemeral temp dir. Fixed: bundle `google_credentials.json` + `collect_all` the google API libs in `valet.spec`; `google_auth` resolves creds from `_MEIPASS` and writes tokens to the writable data dir (persists across reinstalls). New `google_calendar.py` read module; calendar reads **merge Apple + Google and de-dup** the overlap (Google syncs into macOS Calendar, so events would otherwise double — dedup keys on normalized title + UTC minute, date for all-day). Onboarding copy updated (dropped "coming in a future update"; points to Settings → Console Settings → Accounts). Google Cloud side completed by the user: OAuth consent App name renamed Jarvis → "VALET Voice", authorized domain `valet-voice.com`, connected as `finley@qsbsrollover.com`.
- **"open my <X> note" shows it in Notes.app** (b3950a4): new `open_note()` + `_OPEN_NOTE_RE` fast-path; distinguishes "open/go to" (show in app) from "read" (read aloud). `notes_access` factored to share `_resolve_note_title`. Emits process-panel events.
- **"open <website>" opens the URL directly** (8d678fe): new `_WEB_DESTINATIONS` map + `_OPEN_WEB_RE` fast-path → `open_url` → `open_browser` (`open -a`, no System Events keystrokes / Accessibility). Fixes "open my google calendar" failing with `-1743/1002` and "open Google Analytics" doing nothing.
- **Notes fuzzy-match robustness** (0fac61b, c6c9717): added `_recent_titles()` (names only, no container read) for the `read_note` normalized fallback; `get_recent_notes` guards the per-note container read against `-1728`. Initial fuzzy match normalizes spaces/case/punct so "on boarding" == "Onboarding". DRAFT_EMAIL gives an honest "Gmail not connected" message up front (checks `google_auth.is_connected()`).
- **Confirm-flow deadlock fixed** (1e3a028): calendar create/cancel + send hung until the 120s timeout — the WS receive loop awaited the confirmation future inline, so it never read the `confirm_response` reply it was waiting for. Fixed by running confirm+dispatch on a background task (`_confirm_and_dispatch`).
- **macOS permissions inventory** (this branch): Permissions panel now lists Microphone, Full Disk Access, Calendar, Automation, **Contacts** (new this session), Accessibility (post-v1). Calendar + audio-input were added in prior sessions; Contacts joins them.

### Stage K / UC1 — Universal control: Accessibility primitives (MERGED to main — #80)
First stage of the universal screenshot/AX control track (UC0 audit → UC1…UC6). Builds the general perception→action input layer **behind the existing `ActionExecutor`/`Capability` seam** — additive, AppleScript path untouched.
- **New control primitives** in `accessibility_executor.py` (rewritten from the K1 keyboard-only stub): `observe_ui` enumerates the focused window's AX tree (via `ApplicationServices` `AXUIElementCopyAttributeValue`) into a ref-addressable element list — `role`, `title/label`, `value`, `enabled`, `frame [x,y,w,h]`; `click_element(ref|point)` clicks via **AXPress where available, else a synthetic `CGEvent` mouse click** at the element centre; `key_combo("cmd+s")` posts modifier chords (US-keycode map); `send_keystroke`/`open_app` carried over. Import-safe (pyobjc absent → clean `not_supported`, never raises) and a real `AXIsProcessTrusted` gate (untrusted → a failure that names the permission, never a silent no-op).
- **Seam additions** (`action_executor.py`): `Capability.{OBSERVE_UI, CLICK_ELEMENT, KEY_COMBO}` + a `UIElement` dataclass + three **concrete-default** methods returning `not_supported`, so existing backends (AppleScript) inherit the default untouched. `composite_executor.py` routes the three to the AX fallback; `safe_executor.py` gates them.
- **Safety tiering** (`safety.py`): `OBSERVE_UI` = Tier 0 (read, runs straight through, still kill-switch-aware); `CLICK_ELEMENT` + `KEY_COMBO` = **Tier 1** (confirm card + kill switch) — a synthesized click/keypress mutates the world, so it confirms exactly like delete/keystroke.
- **Wiring:** the Accessibility backend is now **always** composed under AppleScript (`server.py`: `safe(composite(macos-applescript->macos-accessibility))`), replacing the off-by-default `VALET_UI_FALLBACK` env gate. Safe because the backend is inert without pyobjc + the grant, and AppleScript still owns every capability it already handled (`executor.*` is only used for Tier-1 file ops). Gated `/api/ax/{observe,click,type,key}` endpoints expose the primitives through the same `SafeExecutor` (the integration seam UC4's loop will drive).
- **Accessibility permission, end-to-end:** `/api/permissions/status` now returns a **real** `AXIsProcessTrusted` bool for `accessibility` (was a hardcoded `null` "post-v1" — the same anti-pattern as the mic-green bug) so a revoke flips it back to red; `/api/permissions/trigger` fires the native grant prompt (`AXIsProcessTrustedWithOptions`); Settings → Console → Permissions and the onboarding wizard both show the live status with an inline Enable → Open-Settings → Re-check flow. Fixed a pre-existing **infinite-recursion bug** in onboarding `loadPerms()` (called itself instead of fetching `/api/permissions/status`) that broke the whole permissions step.
- **Packaging:** `pyobjc-framework-ApplicationServices` added to `requirements.txt`; `valet.spec` `collect_all`s Quartz + ApplicationServices + CoreText (the EventKit FULL-collection lesson) so AX/CGEvent symbols resolve in the signed build. Found in passing that `pyobjc-framework-Quartz` (already in `requirements.txt`) was **never installed** in the dev venv, so the K1 AX backend had been silently inert.
- **Tests/validation:** `tests/test_uc1_accessibility.py` (9 headless tests — seam, tiers, routing, gating, import-safety, key parsing — all pass). `scripts/ax_smoke.py` for on-device runs. **`observe_ui` live-validated on this machine** (Accessibility-trusted): Finder → desktop + icons; a Chrome window → 250 real elements with correct roles/titles/frames. Synthetic click/type/key are import-validated + unit-tested but **need on-device acceptance in the signed build** (not fired into the live desktop here).

### Stage K / UC2 — Screen perception / observation (MERGED to main — #82, landed via #87)
Turns "what's on screen" into a single structured **observation** = focused-window screenshot **+** the UC1 AX element snapshot — the foundation UC3's "click on Submit" stands on.
- **New `perception.py`:** `capture_focused_window()` finds the focused window's `CGWindowID` (`CGWindowListCopyWindowInfo`, frontmost normal-layer window for the app's pid), captures just that window (`screencapture -l <id> -o -x`), and downscales to a token-sane 1366px longest edge (`sips -Z`). `build_observation(executor)` fuses that image with `executor.observe_ui()` (the UC1 AX snapshot) into one observation `{app, elements, window_frame, image, screen_recording, ax_ok}`. `describe_observation()` sends image + numbered AX element list to the model and returns a short spoken read; AX-only and title-only fallbacks when there's no image/client. Import-safe (pyobjc absent → no image, never raises).
- **Screen Recording permission, end-to-end:** real `CGPreflightScreenCaptureAccess` status in `/api/permissions/status` (new `screen_recording` entry) — so a revoke flips it red; native prompt via `CGRequestScreenCaptureAccess` in `/api/permissions/trigger`; Settings → Console → Permissions + onboarding both render it with an inline Enable → Open-Settings → Re-check flow. No new dependency — the Screen Recording + `CGWindowList` APIs are already in the UC1-bundled Quartz.
- **Wiring:** `server.py` `_do_screen_lookup` (the voice "what's on screen" path) now builds the observation and describes it via the proxy, falling back to the old whole-display describe then a window-list summary. New read-only `/api/perception/observe` endpoint (Tier 0) returns the observation with **image metadata only** — the base64 bytes never enter the response or logs (an observation for the model, never an analytics payload).
- **Tests/validation:** `tests/test_uc2_perception.py` (7 headless — fusion, AX-text rendering, no-image/no-client fallbacks, import-safety; all pass). **Live-validated on-device:** focused-window observation of TextEdit = 42 AX elements + a 1366×1137 PNG (~106 KB); and the full vision path — Haiku read back the actual on-screen text ("Quarterly report draft. Revenue up 12%…") and named the app/toolbar accurately. Pending on-device: the Screen Recording inline-grant/revoke→red UI in a running build.

### Stage K / UC3 — Click/type by natural-language description (MERGED to main — #83, landed via #87)
"Click on Submit" / "type my email in the address field" → a concrete, gated action — the sellable demo capability.
- **New `target_resolver.py`:** `resolve(observation, description, client, intent)` is AX-first, vision-fallback. **Primary:** the model picks the single best element `ref` from the UC2 AX snapshot (strict JSON; validates the ref is real, never invents one). **Fallback:** when the AX tree is thin (<4 interactive elements, e.g. Electron/canvas) or the AX pick misses, a vision call returns pixel coordinates, mapped back to absolute screen coords via the observation's `window_frame` + image size, for a point-click. Ambiguous → asks; nothing found → honest miss. Pure/model-driven — decides only; never clicks.
- **Execution (gated):** `server.py` `_resolve_and_act` builds a fresh observation, resolves, emits the **chosen target to the process panel before acting** (supervision), then runs the click/type through `SafeExecutor` (confirm card + kill switch). For 'type' the field is focused via benign AX (`focus_element`, no synthetic input) so only the keystroke raises a confirm. New `/api/ui/act` endpoint + a voice fast-path (`_UI_CLICK_RE`/`_UI_TYPE_RE` → `ui_act` → `_handle_ui_act`, run on a background task so the confirm reply is read — the deadlock-safe pattern).
- **Tests/validation:** `tests/test_uc3_resolver.py` (8 headless — single-match, ambiguous-asks, honest-miss, invalid-ref→miss, AX-miss→vision, thin-AX→vision, pixel→screen mapping, JSON-fence tolerance; all pass). **Live-validated on-device** against real TextEdit: "typeface"→the typeface popup, "the font size box"→the font-size combo, "text color"→the color well, "Submit button"→honest miss (AX miss → vision → not found → no wild click). `scripts/ax_smoke.py act` for on-device repro. Pending on-device: live confirm-card click-through for a resolved target, and an Electron/Chrome surface exercising the vision fallback in a running build.

### Stage K / UC4 — Observe→decide→act loop (MERGED to main — #84, landed via #87)
The multi-step, demo-able capability: give Vee a small goal and it runs a supervised loop until done.
- **New `agent_loop.py`:** `run_loop(executor, goal, client, …)` — **observe** (UC2) → model **decides** the next single action via a **forced tool call** (`next_action` schema: click/type/key/open_app/done/fail — guarantees a valid action, no JSON-parse flake) → **execute** (UC1/UC3 primitives) through the safety gate → **re-observe** → repeat. Returns `{status, steps, history, message}` with status ∈ done/failed/vetoed/halted/capped. Defaults to **Sonnet** for the decide step (Haiku was too unreliable picking the right element step-after-step). Pure orchestration — drives the passed-in executor + client, emits via a caller callback.
- **Supervision foregrounded:** every observe/decide/act beat streams to the process panel (`emit_step`); each mutating step is gated (confirm card + kill switch — the user sees and can veto each step); a **hard step cap** stops runaways; a **veto (denied)** stops the task; the **kill switch** is checked every iteration; a failed action re-observes and lets the model adjust, bailing after 2 consecutive non-veto failures rather than looping blindly.
- **Wiring:** `server.py` `_run_ui_task` + `/api/ui/task` endpoint + a voice path — the LLM emits `[ACTION:UI_TASK] <goal>` for multi-step "do X then Y" UI requests, dispatched to `_handle_ui_task` on a background task (deadlock-safe per-step confirms).
- **Tests/validation:** `tests/test_uc4_loop.py` (6 headless — done-detection, hard step cap, veto-stop, kill-switch halt, stuck-recovery bail, honest model-fail; all pass). **Live on-device:** the loop ran a real 2–3 step TextEdit task end-to-end (type → cmd+a → done) under auto-allowed per-step confirms — actions executed and the model recognized completion. **Known caveat (focus):** synthetic keystrokes always go to the *focused* app; in a CLI test harness a terminal stole focus and the typed text landed there instead of TextEdit. In the real app driving a foregrounded target it lands correctly, and the per-step confirm keeps the user in the loop — but robust app-focusing before typing is a follow-up to harden. Pending on-device: live confirm-card/STOP click-through of a full loop in a running build (incl. the flagged "keep STOP visible during the loop" UI requirement from UC1).

### Stage K / UC5 — Interruptible / barge-in voice (MERGED to main — #85, landed via #87)
From the founding brief: talk over Vee, redirect, or stop mid-stream — not just wake-word + turn-based.
- **The core change is keeping the mic LIVE during TTS.** Today the frontend `pause()`s the mic while Vee thinks/speaks (so you could only barge in with the mouse STOP button). `main.ts` now keeps the recognizer running through `thinking`/`speaking`, and filters echo so Vee doesn't hear itself.
- **New `frontend/src/bargeIn.ts`:** pure `shouldBargeIn(heard, spoken)` — explicit interrupt words ("stop", "wait", "no", "actually", "cancel", …) always cut in; otherwise a transcript that mostly overlaps what Vee is currently saying is treated as echo and ignored; a substantive non-echo utterance barges in. `main.ts` tracks `currentSpokenText` (from the audio message's `text`) to feed the filter; on a real barge-in it stops TTS, sends a `barge_in` signal, then the new transcript.
- **Backend `barge_in` (server.py):** cancels the in-flight reply (`_cancel_response`) **and** the running UC3/UC4 loop/action task — **without latching the kill switch** — so the next utterance runs immediately (a redirect, not a dead stop). The loop/act tasks are now tracked per-WS (`_track_uc`) so they're cancellable; a fresh command also cancels a running loop (belt-and-suspenders). Handled inline on the WS receive loop (just flips a flag + cancels a task), so the loop never parks — the confirm-deadlock lesson re-verified.
- **Tests/validation:** `bargeIn` decision logic verified (10/10 cases: keyword interrupts, echo suppression, new-command vs echo, thinking-state). Frontend typechecks + builds; backend compiles; the 30 UC1–UC4 headless tests still pass. **Pending on-device (needs live speech):** the actual talk-over-TTS interrupt within ~1s and rapid interrupt/redirect — echo behaviour is mic/speaker-dependent and will need live tuning. **Scope note:** barge-in currently *cancels* the running loop and runs the new utterance fresh; injecting a correction into a still-running loop without restarting ("no, the other one" mid-step) is a refinement on top of this.

### Stage K / UC6 — Skills as accelerators: terminal + Cursor (MERGED to main — #86, landed via #87)
Formalizes the existing fast-paths as gateable **skills**, and adds the net-new ones Jacques named.
- **New `skills.py` — the free/paid seam:** a registry naming every accelerator (open app/site/note, calendar, mail, draft-email, research, the UC4 loop, …) and tagging each FREE or PAID, plus `gate(name, plan)` — the single place a future paywall reads (reports the boundary, **enforces nothing**, no billing logic). `/api/skills` exposes it. Existing fast-paths are repositioned, not rebuilt — "start an email to Nick" is still the existing `DRAFT_EMAIL` skill, now cataloged.
- **New `terminal_skill.py` — voice terminal commands, safety-tiered:** `classify_command` puts read-only commands (git status/log/diff, ls, pwd, version flags, …) at **Tier 0 (auto)** and everything else — including any shell metacharacters — at **Tier 1 (confirm card)**, with `danger_warning` flagging clearly destructive ones (rm -rf, sudo, force-push, …). `run_command` executes with a timeout and captures output. Voice: "run npm install" / "run git status" (the verb only matches a known-tool first word, so "run the tests" stays conversation).
- **Cursor skills (built on the universal layer):** `cursor_goto` opens a file at a line via the `cursor://file/<path>:<line>` deep link; `cursor_symbol` drives Cursor through UC1/UC3 — activate → Go-to-Symbol-in-Workspace (⌘T, Tier-1 gated) → type the symbol. Voice: "open server.py at line 42 in cursor", "search symbol run_loop in cursor". `/api/skill/{run,cursor/goto,cursor/symbol}` endpoints.
- **Tests/validation:** `tests/test_uc6_skills.py` (9 headless — command tiering incl. metacharacter/destructive cases, the registry free/paid seam + gate, the Cursor deep-link, and the voice regexes; all pass). **Live-validated on-device:** `git status`→Tier 0 (ran, returned the branch), `npm install`→Tier 1; registry 6 free / 12 paid; gate reports `would_gate` for a paid skill on a free plan; cursor URL builds correctly. Pending on-device: live confirm-card for a Tier-1 command and the Cursor symbol-search in a running build.

### Universal Control track (UC0–UC6) — summary + ship proposal
**The whole track is now on `main`** (this session, 2026-06-12): the general perception→action layer the MVP needed, built *behind the existing `ActionExecutor`/`Capability` seam* (additive — the AppleScript path is untouched).
- **UC1** Accessibility executor — AX-tree enumeration (ref/role/label/value/enabled/frame), click (AXPress-first, CGEvent fallback), type, key combos; real `AXIsProcessTrusted`. **UC2** screen perception — focused-window screenshot + AX snapshot fused into one observation; Screen Recording permission. **UC3** "click on Submit" — model picks a ref (AX-first, vision pixel→screen fallback), ambiguous→ask, miss→honest. **UC4** observe→decide→act loop — Sonnet + forced tool-call, per-step confirm + kill switch + step cap + recovery. **UC5** barge-in — mic live during TTS, echo-filtered interrupt that cancels in-flight without latching kill. **UC6** skills — free/paid registry, safety-tiered voice terminal, Cursor goto/symbol.
- **Cross-cutting:** every mutating action is Tier-1 gated (confirm card + always-live kill switch); two new TCC permissions (Accessibility, Screen Recording) plumbed end-to-end with real checks (replacing the old hardcoded-null); `ApplicationServices` added to deps + spec; **39 headless tests** + extensive on-device validation; honest caveats logged (synthetic-input focus, live barge-in tuning, in-app confirm/STOP/permission-UI passes).
- **How it landed (a wrinkle worth remembering):** #80 (UC1) and #81 (wake-on-"av") merged to main directly. UC2–UC6 (#82–#86) were **stacked PRs** that each merged into their parent branch rather than main, so only UC1 initially reached main. **PR #87** (`feat/uc5-barge-in` → main) then landed all of UC2–UC6 in one go — 6 merge conflicts resolved by taking the superset branch, the #81 wake-av fix preserved, 39 tests pass, tsc clean. Lesson: stacked PRs must retarget `main`.
- **Not yet packaged:** no signed/notarized build has been cut with any of this. **Open caveats before a UC4/UC6/UC5 build:** synthetic-input target-focus hardening (keystrokes follow keyboard focus); live in-app passes (UC4 confirm-card/STOP click-through, Screen-Recording grant/revoke UI, UC5 echo tuning on real hardware); and the STOP-stays-visible-during-the-loop UI fix (`main.ts`).
- **Proposed next signed build:** ship UC1–UC3 first (observe + click/type-by-description are reliable and demo-clean), gated behind the Accessibility + Screen Recording grants in onboarding. Hold UC4 (loop) and UC6 terminal for a build *after* (a) the synthetic-input target-focus hardening and (b) the live confirm-card/STOP/permission-UI passes in the signed app, and UC5 after a live echo-tuning pass on real hardware.

### Proxy analytics + checkout
- **Privacy-respecting action analytics** (`product-site/lib/proxy/anthropic.ts`, `langfuse.ts`; PRs #39, #40). A streaming transform extracts `[ACTION:TYPE] target` tags from the assistant's reply and logs them to Langfuse as trace tags + metadata (bare type, e.g. `open_app`, plus `app:Spotify`-style target tags). No raw prompts or responses are stored (`PROXY_CAPTURE_PAYLOADS` off by default); sensitive targets (file paths on deletes/builds) are dropped, only app/project names kept.
- **Marketing site** (PR #38): cinematic 5-page landing — home (particle orb + 01-03 sequence visuals + live-demo terminal + capabilities), `/how-it-works`, `/pricing`, `/faq`, `/contact` — plus new `/privacy` and `/terms`. Langfuse MCP connected for analytics; usage dashboard planned, not built.

### Carried over (pre-Phase-2, still live)
- **Frontend / UI:** audio-reactive Three.js orb (`frontend/src/orb.ts`); draggable holographic process panel (`processPanel.{ts,css}`); design panel with "+ New project…" flow (`designPanel.{ts,css}`); floating result cards (`floatingPanels.ts`); settings (`settings.ts`); wake-phrase detection — "ok vee" / "hey vee" + soft "vee?" (`wakeWord.ts`).
- **Backend / data:** FastAPI WebSocket server (`server.py`); tag-driven action dispatch (`[ACTION:BUILD|BROWSE|RESEARCH|CHECK_WEATHER|PROMPT_PROJECT|ADD_TASK|REMEMBER|OPEN_PROJECT]`, `[DISPATCH_TO_AGENT]`); fast-path intent regexes; native research via Opus web tools; design partner (`design_partner.py`); weather pipeline (`weather.py`, Open-Meteo); SQLite + FTS5 memory (`memory.py`); process event bus (`process_events.py`); Calendar / Mail (RO) / Notes via AppleScript; Playwright browser (`browser.py`); persistent Claude Code sessions (`work_mode.py`); greenfield scaffolds (`actions.py`).
- **Infrastructure:** supervised PR factory (`.github/workflows/factory.yml`, two required checks, merge policy in `CLAUDE.md`); macOS launchd service (`scripts/com.valet.backend.plist`, `scripts/valet-launchd.sh`); local SSL self-signed certs for Web Speech API; screenshots via FastAPI StaticFiles; smoke test (`scripts/smoke_test.sh`).

---

## Tech stack  [rewrite]

| Layer | Technology | Notes |
|---|---|---|
| Backend (app) | FastAPI + Python 3.11+ | `server.py` (voice-dispatch core kept here; low-risk concerns split out) |
| Frontend (app) | Vite 6 + TypeScript 5.7 + Three.js 0.183 | `frontend/` |
| Communication | WebSocket (JSON + binary audio) | WSS over self-signed certs |
| Proxy / marketing site | Next.js on Vercel | `product-site/` (https://jarvis-y.vercel.app) |
| AI (fast) | Claude Haiku 4.5 (via proxy) | conversation; streamed (speak-on-first-sentence, #91); $1 / $5 per MTok |
| AI (deep) | Claude Opus 4.8 (via proxy) | research; $5 / $25 per MTok |
| TTS | Fish Audio (via proxy) | VALET voice — two British models (Male / Female); keep-alive proxy client + env latency knob (`FISH_LATENCY`/`FISH_MODEL`, #93/#94) |
| App form factor | Tauri menu-bar app | `ActivationPolicy::Accessory` (no Dock icon), tray icon/menu, always-on-top draggable orb popover, `prevent_exit` (#135/#136); `src-tauri/src/main.rs`. Test via a signed debug `.app` bundle, NOT `tauri dev` |
| STT | Web Speech API (Chrome in dev, WKWebView/Apple in the signed build) | client-side, free; staying here pending on-device finalization-latency measurement (#95). **Hold ⌃⌥ from any app = push-to-talk** (#135 chord, supersedes bare-⌥); always-listening wake-word is a toggle |
| Global PTT chord | `core-graphics` `CGEventTap` in the Tauri MAIN process (Rust) | system-wide ⌃⌥-hold (`spawn_global_chord`), drives the frontend via `window.__valetChord` (#135/#136); needs macOS **Input Monitoring** (stable, one signed executable). Backend `global_ptt.py` tap retired |
| Voice-native Raycast | `file_index.py` / `settings_index.py` / `system_actions.py` + `app_index.py` | no-LLM search: app launch (#116), Spotlight files (#137/#138), settings deep links, tiered system actions; via `detect_action_fast` |
| Guided walkthroughs | `walkthrough.py` | teach-don't-do — glide the cursor to each step's control, narrate, wait + re-observe until the user does it (#139, open) |
| Native cursor overlay | Tauri (`macOSPrivateApi`) + Rust | transparent click-through always-on-top window + CSS dot, moved ~60fps by a Rust cursor poll via `window.eval()` (#126); **multi-monitor** — one overlay per display in logical points (#136); dev/debug-bundle only |
| Accounts + Licensing DB | Supabase Postgres (Pro) + Supabase Auth | `licenses` (+ `customer_email`, `user_id`), `license_usage`, `account_sync`, `device_settings` (#66, migration not yet applied) tables; email/password auth via `@supabase/ssr`; custom SMTP via Resend |
| Observability | Langfuse | license as user id, payloads scrubbed |
| Billing | Stripe (LIVE) | Free / Pro $20/mo / Ultra $50/mo — live mode; payouts paused pending bank account |
| Control | `action_executor.py` ABC + AppleScript backend | portable; Windows/Linux = swap |
| Memory | SQLite + FTS5 | `data/` |
| macOS integrations | AppleScript + EventKit + CNContactStore | Calendar (EventKit r/w + Google merge), Mail (RO), Notes, Contacts (RO) |
| Geocode + Weather | Open-Meteo | free, no API key |
| Packaging | Tauri + PyInstaller (`--clean`) | signed (Developer ID, Team QZX7VBLDZT) + notarized (`valet-notary`); `packaging/build-macos.sh` |
| Distribution | GitHub release | `Kuba-Ventures/valet-downloads` `v0.1.0` → Vercel `DOWNLOAD_URL` |

---

## Integrations & MCPs  [rewrite]

| Integration | Purpose | Cost | Status |
|---|---|---|---|
| Anthropic Claude API | Haiku 4.5 conversation, Opus 4.8 research — via the proxy | Haiku $1/$5, Opus $5/$25 per MTok | live (server-side keys) |
| Fish Audio TTS | VALET-voiced replies (two British voices) — via the proxy | ~$15 / 1M chars | live (server-side keys) |
| Supabase | accounts (Auth) + license/usage/sync store (`licenses`, `license_usage`, `account_sync`) | Pro plan | live |
| Langfuse | proxy tracing + privacy-respecting action analytics (tags/metadata, no raw payloads) | unknown | live |
| Langfuse MCP | analytics access for a planned usage dashboard | unknown | connected (dashboard planned) |
| Stripe | checkout + subscriptions + Billing Portal (Free / Pro $20/mo / Ultra $50/mo) | per-transaction | live (Twin Peaks Labs; payouts paused) |
| Resend | transactional email — license key on purchase + Supabase custom SMTP for confirmation/reset | unknown | live (domain `valet-voice.com` verified; `RESEND_API_KEY`/`EMAIL_FROM` set; SMTP `smtp.resend.com:465`) |
| Vercel | hosts `product-site/` (marketing + proxy); `DOWNLOAD_URL` → release DMG | unknown | live |
| GitHub Releases | hosts the signed/notarized DMG (`Kuba-Ventures/valet-downloads` `v0.1.0`) | free | live |
| Open-Meteo | geocoding + weather | free | live |
| Apple Calendar / Mail / Notes / Contacts | local access — Calendar via EventKit (r/w), Mail read-only, Notes via AppleScript, Contacts read-only via CNContactStore | free | live |
| Google (Gmail + Calendar) | per-user OAuth — Gmail drafts + Google Calendar reads merged/de-duped with Apple | free (Google API) | live (creds bundled, token persists; connected as finley@qsbsrollover.com) |
| Playwright (Chromium) | web automation | free | live |
| Claude Code CLI | dispatched sub-agent tasks | inherits Claude plan | live |
| Cursor | paste target for design-panel ship handoff | external app | live |

*Source: no MCP config files found in repo (`.mcp.json` / `mcp.config.*` / `claude_desktop_config.json` absent). Integrations derived from `product-site/lib/proxy/*.ts`, `licensing.py`, `server.py`, `requirements.txt`, and `.env.example`.*

---

## Decisions log  [append-only — never rewrite or delete]

- **2026-06-21 — Settings information architecture: app surfaces aligned to the three-surface IA (branch `feat/settings-ia`)** — One source of truth per setting across web account (identity/billing/license — owns), in-app panel (per-machine config the app validates/applies), and menu bar (status + toggles + links, never a form). This pass aligns the APP surfaces (web-account additions deferred). Decisions: **(1) Secrets = license/proxy** (unchanged) — the in-app local secret is the LICENSE KEY (+ Test), set by account sign-in with manual paste as the **offline** fallback; no raw Anthropic/Fish keys in the app. **(2) Billing is web-owned (invariant 2)** — removed `ACCOUNT_PLAN` from the `/api/settings/keys` writable allowlist so the app can never forge entitlement locally; plan is only ever applied by `/api/account/login` from the server's truth. The Console "License" field is reframed as **offline/advanced activation** with a "manage at your account ↗" link; the read-only plan/license display already lives in the User tab. **(3) Menu bar = toggles/status/links only (invariant 3)** — rebuilt the tray to **Open VALET · Toggle Listening · Settings… · Account ↗ · Restart server · Quit**; Toggle Listening evals the SAME path as the in-orb toggle (one source of truth), Account ↗ opens the web account, Restart hits `POST /api/restart`. **(4) Input Monitoring** added to `/api/permissions/status` (real `CGPreflightListenEventAccess` check + `Privacy_ListenEvent` deep link) so it appears in the panel (dynamic) and onboarding — without it the ⌃⌥ chord silently no-ops on a fresh install (Stage-4-critical). **Deferred:** wake-word + personality editors (need name-propagation / brand-voice care), live tray status label + "Ultra · keys OK" glance (needs frontend→Rust IPC), and the web-account additions (license regenerate, device seats, contacts/team, deletion, data export — a separate product-site PR; Stripe billing portal already exists). Headless green (test_settings_ia 4/4; walkthrough/stage2/app_index unregressed; tsc + cargo check). Touches license/billing/permissions → escalate-tier, human-reviewed PR.
- **2026-06-21 — Stage 3: guided walkthroughs (teach, don't do) (branch `feat/walkthroughs-stage3`)** — Given a goal ("show me how to turn on Bluetooth"), VALET plans steps, glides the **visible cursor to each control without clicking**, narrates, then **waits + re-observes until the user does it** and advances. New `walkthrough.py` reuses the proven seams (no rebuild): `perception.build_observation` (UC2), `target_resolver.resolve` (UC3), `accessibility_executor.glide_to_target` + `emit_cursor_control` (the point-and-teach #113 / follower #126 gesture — glides, never clicks), `process_bus`/`emit_step` + `_speak`, and `_track_uc`/barge-in/`kill_switch` for cancellation. Decisions: **(1) Authoring = curated seeds + model** — `match_curated` (Bluetooth/Wi-Fi/FileVault) for demo reliability, else `plan_steps` (forced-tool Sonnet JSON, mirrors `agent_loop._decide`). **(2) Advance = auto-detect + voice fallback** — pure `step_done(before, after, step)` heuristic (verify-text now visible / app switch / new elements) auto-advances; on stall it re-narrates and accepts "next" / "do it for me" / "stop" via `_walkthrough_signal` intercepted in the WS loop while `ws._wt_active`. **(3) Runaway guard** — `_await_step` re-narrates at `timeout` and hard-caps at 4×timeout → `stalled` (never spins forever). **(4) Teach-don't-do** — the loop only points; the sole click path is an explicit "do it for me" → existing **gated** `_resolve_and_act` (confirm card + kill switch). **(5) Testability** — `run_walkthrough` takes all I/O as injected callables, so the loop is unit-tested with scripted observations/fakes (no real screen/cursor/LLM). Detect runs BEFORE UC3 point-and-teach so "show me how to …" isn't mistaken for "show me <element>". Headless green (test_walkthrough 12/12; uc3_point/uc4_loop/stage2 unregressed). On-device validation via the signed bundle pending. Backend Python → escalate-tier, human-reviewed PR.
- **2026-06-21 — Stage 2: voice-native Raycast — no-LLM file / settings / system-action search (branch `feat/voice-search-stage2`)** — Broadened the no-LLM fast path (`detect_action_fast` → action dict → executor) beyond app launch. Three new pure, unit-tested modules mirror `app_index.py`: **`file_index.py`** (Spotlight `mdfind`, home-scoped + timeout-capped so a cold query never blocks the hot path; pure `rank_hits` + `detect_kind`), **`settings_index.py`** (`x-apple.systempreferences:` deep-link map + fuzzy `match_setting`), **`system_actions.py`** (list-form commands tagged with a safety tier). Wiring decisions: **(1) Placement/precedence** — settings + file matchers run AFTER every domain matcher (calendar/mail/notes/web) but BEFORE the greedy `open_project` route; system actions run near the end (they never start with "open"). **(2) File-intent gating** — to avoid hijacking "open my X project", file search needs a real cue: a browse verb ("find/locate X") OR a kind word ("…pdf/spreadsheet/screenshot") OR the word file/document. A bare "open my X" stays a project. **(3) Disambiguation** — multiple file hits set `ws.pending_offer = {"kind":"file_pick"}` and the user picks BY VOICE ("the second one"), reusing the ordinal parser in `_handle_pending_offer`; a clear winner (score gap ≥25) opens directly. **(4) Scope ceiling** = apps + files + settings + system actions; window-management/clipboard (#69) is OUT. **(5) Destructive system actions** (empty trash / restart / shut down) are included but Tier-1 — routed through the existing `confirmations.request` card + kill switch from the executor (a background task, so the confirm reply never parks the WS loop); safe actions (lock/sleep/screensaver/volume) are Tier 0. **(6) Latency** — `find_file`/`open_settings`/`system_action` dispatch is wrapped in `_timed_action` so `/api/latency/last` records them (sub-second, no LLM marks). New skills registered for the `gate()` seam. Headless green (3 module suites + `detect_action_fast` routing 8/8; app_index 11/11 unregressed). On-device validation via the signed debug bundle pending. All backend Python → escalate-tier, human-reviewed PR.
- **2026-06-21 — Stage 1 on-device hardening: global ⌃⌥ tap moved to the SIGNED main app; cursor follower is multi-monitor; menu-bar lifecycle fixed (branch `fix/tray-icon-panic`)** — On-device testing of Stage 1 surfaced a cluster of macOS-native issues, each fixed: **(1) Tray icon panic** — `default_window_icon().unwrap()` is None in dev → embed `icons/32x32.png` via `include_image!`; tray-build is now non-fatal. **(2) Menu-bar lifecycle** — a tray app must not quit when its window closes: added `RunEvent::ExitRequested → prevent_exit()` and CloseRequested on `main` hides (not closes) the orb. **(3) `tauri dev` can't run this app on macOS** — the webview auto-starts Web-Speech STT, a TCC-gated resource, and TCC only honors `NSSpeechRecognitionUsageDescription` from a real `.app` bundle's Info.plist, NOT a loose/embedded binary → the dev binary is SIGABRT'd ~3s in. **So the demo cadence (decision #4, 2026-06-20) is revised: test from a debug `.app` bundle (`tauri build --debug`), not `tauri dev`.** **(4) Global ⌃⌥ "from anywhere" required two corrections:** the tap was in the backend helper (`global_ptt.py`), but macOS Input Monitoring is granted PER EXECUTABLE and the helper's hash churns every build → it silently received zero key events. Moved the tap into the Tauri MAIN process (`spawn_global_chord` in `src-tauri`, a raw `core-graphics` `CGEventTap` — NOT `rdev`, which resolves keycodes via main-thread-only HIToolbox and aborts off-main). It drives the frontend via a `window.__valetChord` eval hook; `global_ptt.py`'s sidecar tap is retired. AND the dev build must be **codesigned with the Developer ID** (stable identity `ai.valet.desktop` / team QZX7VBLDZT) so the Input Monitoring grant PERSISTS across rebuilds — unsigned dev binaries churn cdhash and lose the grant every build. (The shipped DMG already signs, so this "just works" in production; the manual `codesign` is a dev-loop step.) **(5) Multi-monitor cursor follower** — one click-through overlay per display, placed and hit-tested entirely in the global LOGICAL point space (`monitor.position()` is points, `monitor.size()` is physical px; `cursor_position()` is points×primary-scale), since that's the only space macOS keeps consistent across mixed-DPI displays. **(6) Polish:** orb drag is a visible top header (full-window drag ate the click that unlocks the AudioContext → no TTS audio); loading screen is draggable; follower dot shrunk to 8px and offset down-right of the pointer; in-window PTT switched from bare-⌥ to the ⌃⌥ chord. All escalate-tier → human-reviewed PR.
- **2026-06-20 — Menu-bar restructure Stage 1: the front door is a menu-bar app + ⌃⌥-chord PTT, with an always-visible orb (branch `feat/menu-bar-shell-chord-ptt`)** — First stage of the menu-bar product restructure (`coding-plans` staged build). Decisions locked with the sponsor's mandate (native + fast + Clicky): **(1) Chord = ⌃⌥ held, hold-to-talk** — supersedes the bare-⌥ global PTT (#122/#134) and the deferred #125 ⌥-Space `global-shortcut` plan; bare ⌥ collided with Option-typing, ⌃⌥ is rarely held alone. **(2) The orb is ALWAYS visible unless the user hides it; visibility is controlled from the menu-bar tray, NOT by the chord** — the chord only opens the mic, it never summons/dismisses the orb (this is the dismiss-behavior call: no auto-dismiss, no `NSPopover`). The orb window is the existing `main` webview repurposed as a frameless, always-on-top, skip-taskbar 380×560 widget; position persists via `tauri-plugin-window-state` (the fullscreen cursor `overlay` window is excluded from restore and stays fully independent). **(3) ⌃⌥ collision → cancel-on-other-key** — the chord fires PTT on ⌃⌥-down but if any non-modifier key lands mid-hold (a real ⌃⌥-letter shortcut) the turn is cancelled and the captured audio discarded, so app shortcuts still work. Implemented as a new `on_change("cancel")` state from `global_ptt` → `wake.cancelPushToTalk()` (flush recognizer buffer, swallow the trailing FINAL via a short discard window, mic stays listening). The `global_ptt` chord state machine was extracted into pure `_on_flags`/`_on_keydown` helpers (10 headless tests). **(4) No Dock icon** via `ActivationPolicy::Accessory`. **(5) Demo cadence = `tauri dev` per stage; signed DMG is the Stage 4 capstone.** Deferred to later stages: full orb transparency (kept opaque for now to avoid a frontend background overhaul), Input-Monitoring permission onboarding wiring (Stage 4), and a dedicated tray "Permissions" deep link (Stage 1 just shows+focuses the orb). Drag uses `data-tauri-drag-region` on a top strip; because the UI is served from a remote origin (`http://localhost:8340`) this needed a new `capabilities/orb-drag.json` granting `core:window:allow-start-dragging` to that origin only. Headless gates green (pytest 10/10, `tsc`+vite build, `cargo check`); on-device validation pending (human — chord/no-Dock/drag-persist can't be verified headlessly). All Stage 1 surfaces are escalate-tier (Rust, Python, Tauri/Cargo config) → human-reviewed PR, no auto-merge.
- **2026-06-20 — Global push-to-talk via a backend Quartz CGEventTap broadcasting over the existing WS, NOT a native Rust/Tauri shortcut** — Hold-⌥ must work in ANY app, but the in-window keydown can't see keys when another app is focused. Two routes were on the table: (a) the eng-review's planned Tauri `tauri-plugin-global-shortcut` (which forced a ⌥-Space *chord* because bare ⌥ isn't registerable as a Tauri shortcut), or (b) a backend listen-only `CGEventTap` watching the Option flag system-wide. Chose (b): `global_ptt.py` runs the tap on a daemon thread with its own `CFRunLoop` and, on each Option transition, hops onto the asyncio loop via `run_coroutine_threadsafe` to `process_bus.broadcast({"type":"ptt",...})` — reusing the connected-WS fan-out; the frontend then takes the *exact same* idempotent `beginPushToTalk`/`endPushToTalk` path as the in-window ⌥. Rationale: it supports **bare ⌥ hold** (the natural Clicky gesture, not a chord), it's listen-only so Option keeps working everywhere, and it needs no new frontend capability — a generic `broadcast()` on the bus carries any control frame, so `emit()` just delegates to it. An NSEvent global monitor was rejected because the backend runs an asyncio loop, not a Cocoa run loop, so it would never fire. The cost recorded as an open loop: this needs macOS **Input Monitoring** (a distinct grant from Accessibility), and the shipped sidecar's TCC story must be verified at DMG time. This **supersedes** the #125 `tauri-plugin-global-shortcut` plan for bare-⌥ hold-to-talk; one of the two must be removed before shipping.
- **2026-06-20 — Render the cursor dot as a CSS transform in a transparent click-through overlay, not per-frame window moves or a Swift sidecar (#125/#126)** — The native cursor follower (the real Clicky look) is one fullscreen transparent always-on-top click-through Tauri window; a ~60fps Rust loop reads the global cursor and moves only the dot's CSS transform via `window.eval()` (Rust→webview, no JS capabilities). Moving the *window* per frame is the canonical cursor-follow stutter, so only the dot moves; a Swift sidecar was rejected to stay in one stack. Required enabling Tauri `macOSPrivateApi` + the `macos-private-api` Cargo feature (transparent windows). Click-through (`set_ignore_cursor_events(true)`) is the one critical gap flagged in review — unverified, it eats every click.
- **2026-06-20 — A `muted` (hard-stop) flag is separate from `isSleeping` (wake-mic-off); audio gates on `muted` (#127)** — #118 silenced TTS instantly on Escape by returning early when `isSleeping`, and #122 made PTT-primary the default with `isSleeping=true` (wake mic off). Together they meant Vee never spoke in everyday PTT mode. The fix splits the two concepts: `isSleeping` means "wake mic off" (Vee must still speak), and a new `muted` flag is the hard-stop the audio handler actually gates on — set by Escape, cleared on wake/command. The lesson recorded: "asleep" and "muted" are different states and must not share a gate.
- **2026-06-20 — Force Chromium's full AX tree (`AXManualAccessibility`/`AXEnhancedUserInterface`) so web clicks resolve by ref, not vision (#128 + uncommitted coverage bump)** — Chromium only exposes its full accessibility tree to an assistive tech that asks; without it `observe_ui` saw only the toolbar and web clicks fell back to imprecise vision. Setting both attributes (one-time per pid, with a build wait on first touch, cached so repeat observes skip it) makes web elements resolvable. The coverage follow-up records the second half of the lesson: even with the full tree, the resolver only showed the model the first 80 elements (`_elements_block` limit) and capped the AX walk at 250 — on a rich page like GitHub the nav icon and avatar sit past those, so they were never offered to the picker. Raising to 200 / 400 (and the build delay 0.35→0.6s for big pages) is the fix; both bottlenecks are limits, not logic.
- **2026-06-18 — Speed is the product; Clicky is a foundation, not a feature checklist** — Sponsor mandate (Jacques Arnoux, Twin Peaks weekly): make the agent FAST + intuitive, benchmark Hey Clicky, adopt Clicky as the foundation, deprioritize billing/admin. The benchmarking surfaced the key correction: open-source Clicky **teaches/points** but doesn't execute — VALET's Universal Control already **clicks** — so the differentiator is execution speed and intuitiveness, not parity with Clicky's surface. The session's whole shape (no-LLM voice console, visible cursor glide + click, PTT-primary, frictionless no-confirm clicks, faster misses) follows from this. Billing/admin de-prioritization (incl. "payouts paused") is therefore a deliberate focus call, not a crisis.
- **2026-06-18 — Push-to-talk is the DEFAULT activation; always-listening is a toggle (PR #122, supersedes #92's opt-in-off default)** — Wake-word recognition + the inter-utterance pause are real latency at the front of every turn for the focused use case, plus the "hey vee"→"av" misfire class. Hold-⌥ PTT removes both. So PTT flips from #92's opt-in-off to the default; always-listening (wake-word) becomes a toggle for the hands-free case. The wake-word path is preserved, just no longer the default.
- **2026-06-18 — Voice clicks run with no confirm panel, but the kill-switch is always honored (PR #120)** — For a fast, intuitive feel the per-action confirm card is dropped for voice clicks (they go through the raw AX executor). Safety is not abandoned: the kill switch still cuts in, and #117's **AX hit-test before clicking** means a click only fires when the target is actually under the cursor. Misses were also cut from ~5s to ~1-2s by running the vision pass at most once and making the "X in app" retry AX-only. This trades a confirm prompt for speed on a low-tier action (a single click) while keeping the latching STOP and a structural hit-test guard.
- **2026-06-18 — Visible cursor glide with a physical-mouse abort + hit-test (PR #117)** — The cursor visibly tweens to the target (the Clicky "you can see it move" feel) rather than teleport-clicking, aborts instantly if the user moves a physical mouse (so the human always wins), and hit-tests the element under the cursor before pressing. Built in `accessibility_executor.py` (`move_cursor`, threaded), wired into `_resolve_and_act`, with an Escape hard-stop.
- **2026-06-18 — "open X" launches apps with NO LLM round-trip (PR #116)** — The Raycast-style instant-launch: a dedicated `app_index.py` fuzzy matcher resolves the app name locally and launches, skipping the model entirely. Handles nested `.app` bundles and is space-insensitive. Speed > generality for the most common command.
- **2026-06-18 — Point-and-teach is structurally input-free (PR #113)** — "where is X" speaks the location and points without moving or clicking anything — a locate-only slice of UC3 that can never mutate the world, so it needs no confirm/kill gating to be safe. `target_resolver` carries the element frame, `_resolve_and_point` + `emit_pointer` render it.
- **2026-06-18 — Enable "require branches up to date before merging" (merge-train safety)** — Two squash-merges silently dropped a parent's commits this session: #114 dropped #113's `server.py` edits (restored by #115) and #116's nested-bundle fix was dropped and restored. Stale-base merges can drop commits that aren't in the squashed diff. The fix is the GitHub branch-protection rule "require branches up to date before merging" so a behind-base branch must rebase before it can merge. (Open loop — not yet enabled.)
- **2026-06-15 — Account login activates the license IN-SESSION, no relaunch (PR #102, supersedes the #98 relaunch behavior)** — The #98 design returned `needs_relaunch:true` and told the user to restart to activate a newly-provisioned key, to avoid a fragile runtime re-init of the critical LLM client. Live testing exposed the real failure: a fresh-install session starts with an empty `.env` (no proxy client, empty `LICENSE_KEY` global), so after onboarding login the app said "API key not configured" and couldn't speak at all. The fix is `_reinit_after_license()` — rebuild the proxy LLM client and update the `LICENSE_KEY` global (read by `synthesize_speech` for its `X-License-Key` header) right after a successful login, so LLM + TTS work immediately. **`needs_relaunch` is now always false.** We accepted the targeted runtime re-init rather than shipping a "log in then restart" flow that broke the first-talk experience. Also trimmed from live feedback: removed the onboarding Connections step (informational click-through that connected nothing) and the in-app Contacts editor (replaced with a "manage account & contacts in your browser" link — Apple-Contacts resolution still works; a web contacts editor is the follow-up).
- **2026-06-15 — Onboarding/Settings must not stack setup-mode on the wizard; the wizard wins (PR #101)** — The real root cause of "still asked to log in after onboarding": startup ran BOTH `checkFirstTimeSetup()` (opens Settings in setup mode when unlicensed) AND `maybeShowOnboarding()`, so Settings opened in setup mode behind the wizard and a stale login panel surfaced after the wizard finished (`openSettings` early-returns on `isOpen`, so the exit-setup fix never fired). Fix: `maybeShowOnboarding()` returns whether it showed, and `main.ts` only falls back to `checkFirstTimeSetup()` when the wizard is NOT showing; `exitSetupMode()` is extracted and called whenever the user is licensed. A licensed user always lands in normal tabbed settings. Side effect: the linear setup view that surfaced Contacts is gone — Contacts shows only in Console Settings, as intended.
- **2026-06-15 — Account email/plan are identity fields, not secrets — persist them in .env beside the rest (PRs #99/#101)** — After sign-in the app shows a collapsed "Signed in as <email> · <tier> · License #" state with a Sign out button, rather than leaving the login form next to the populated profile. State persists via new `ACCOUNT_EMAIL` + `ACCOUNT_PLAN` .env keys (written on login, `ACCOUNT_EMAIL` cleared on sign out), returned by `/api/settings/preferences` alongside `license_key`. These sit beside the other identity fields — not secrets. The password is never written. The `[type=email]` field also had to be added to the settings CSS selector (it was rendering white/narrow on the browser default) with a `-webkit-autofill` dark-repaint so it matches every other field.
- **2026-06-15 — Account login in the app: provision license + profile from .env, prompt relaunch to activate the license (account-login PR B)** — App side of the account-login feature (PR A is the product-site backend). Adds a "Log in to account" section under **User Settings** (email + password) → `POST /api/account/login`, which forwards creds to the proxy's `app-login` (PR A), then writes the returned `LICENSE_KEY` + profile (name→USER_NAME, honorific→HONORIFIC, date_of_birth→DATE_OF_BIRTH, location→HOMETOWN_CITY, work/personal email) to `~/Library/Application Support/VALET/.env`. The password is forwarded for verification and **never stored or logged** in the app. **Activation nuance:** profile fields apply live (read fresh per turn) and the license gate re-validates live (`licensing.validate`), but the LLM proxy client + `LICENSE_KEY` global are bound at startup — so a newly-provisioned key returns `needs_relaunch:true` and the UI says "Restart VALET to activate your license" rather than risking a runtime re-init of the critical LLM client. Also added a launch-time `_fetch_and_apply_profile()` (GET `/api/proxy/profile`, **fill-empty-only** so it never clobbers local edits) so subsequent installs auto-populate. Manual license-key entry kept as the offline fallback. Onboarding login + web profile editor deferred. py_compile + tsc/vite clean; route registered; needs PR A deployed to Vercel to function end-to-end.
- **2026-06-15 — STT provider: stay on Web Speech for now; measure finalization latency on the prod WebView before paying for a streaming provider (perceived-latency investigation, no code)** — Write-up in `docs/stt_provider_investigation.md`. Key findings: (1) **PR 0 doesn't actually measure STT finalization latency** — `t0` is *defined as* the STT-final, so the you-stop-talking→final gap is upstream and uncaptured; provided a ~5-line `onspeechend→final` instrumentation to capture it. (2) **Dev and prod use different speech engines** — Chrome/Google in dev, WebKit/WKWebView/Apple in the Tauri build — so any STT measurement or accuracy call must be made in the prod WebView. (3) Web Speech's reliability cost is already visible as workarounds: the entire `wakeWord.ts` mishearing vocabulary (incl. "hey vee"→"av" collapse) and `voice.ts` rapid-end backoff. (4) Cost/latency comparison grounded in current pricing: AssemblyAI Universal-Streaming ~$0.15/hr (~300ms, immutable transcripts, tunable endpointing) is the cheapest/lowest-latency option; Deepgram streaming ~$0.46/hr; both bill session duration, so you'd stream only during active capture. **Recommendation: don't switch on assumption** — Web Speech is free/zero-dep and PR 2 (push-to-talk) already sidesteps the worst mishearing + finalization-pause class at zero cost. Switch only if, after PRs 0–4 land and the instrumentation runs on-device, finalization median is ≳500ms AND accuracy still hurts despite PTT AND the per-user cost is acceptable — then pilot AssemblyAI behind the existing proxy/license pattern, flag-gated, keeping Web Speech as the free fallback.
- **2026-06-15 — Pre-warm + keep-alive the proxy connection; the per-call handshake was the hidden TTS tax (perceived-latency PR 4)** — Found the real, reliable TTS lever the PR-3 audit pointed at: `synthesize_speech` created a **fresh `httpx.AsyncClient` per call**, so every TTS request paid a full TLS handshake to the proxy — **~139ms median measured** (cold ~178ms vs warm ~39ms, against `valetvoice.vercel.app`) — and the Anthropic SDK paid it once on the first turn (both share the proxy host on the licensed path). Fixes: (1) a **shared keep-alive `httpx.AsyncClient`** (`_get_http_client`, pooled, connect-timeout 5s, keepalive 90s) used by `synthesize_speech`, closed in lifespan shutdown — handshake paid once per session, not per turn; (2) a best-effort startup `_prewarm` task that warms the proxy TLS connection (licensed path) **and** the Anthropic SDK→proxy connection + the static prompt cache via one `max_tokens=1` request (disable with `VALET_PREWARM=0`); (3) extracted `_static_system()` so the warm builds a **byte-identical** cacheable prefix → the first real turn gets a cache READ on the ~6–8KB static block, not a cold write. This **compounds with PR 1**: sentence-chunked streaming makes several TTS calls per turn, each of which previously paid its own handshake. Net: ~139ms off every TTS call after the first, and off the very first turn (model + TTS) via the pre-warm. Best-effort throughout — pre-warm never blocks or fails startup.
- **2026-06-15 — Make the Fish latency tier a usable, env-tunable knob — but don't flip the default on assumption (perceived-latency PR 3)** — Audited the TTS path. Fish's `/v1/tts` exposes `latency` (normal|balanced|low) + a `model` header (s1|s2-pro), but the proxy (`product-site/lib/proxy/fish.ts`) **hardcoded the body and stripped both** — so the lever wasn't usable end-to-end. Fixed: the proxy now forwards a validated `latency` (default "normal" when absent → older app builds unchanged) and an optional `model` header; `synthesize_speech` sends them via a pure, unit-tested `_tts_request` builder gated by `FISH_LATENCY`/`FISH_MODEL` env. **Did NOT change the shipping default**: measured full-clip delta on VALET's short single-sentence replies is **within network noise** (~0–12%, run-dependent — `scripts/voice_latency_baseline.py --fish`: e.g. normal 836ms vs balanced 928ms vs low 913ms on one run, the opposite ordering on another), and the app **buffers the whole clip** (`response.content`) so the tier's real time-to-first-byte benefit (streaming) doesn't apply. Flipping the default would trade unverifiable quality for a noise-level gain — so default stays "normal" and the user can A/B `FISH_LATENCY=balanced|low` by ear (env, no rebuild; reversible). **Bigger TTS levers, documented as follow-ups:** (1) true streaming playback (play chunks as they arrive instead of buffering the full MP3) and (2) the proxy/Vercel hop itself (direct Fish ~0.7–0.9s vs proxy ~1.0–2.4s observed in PR 0) — the latter overlaps PR 4's pre-warm. Proxy change is backward-compatible and needs a `product-site` redeploy to take effect on the licensed path; the dev-direct path is immediate.
- **2026-06-15 — Push-to-talk as an instant trigger alongside the wake word (perceived-latency PR 2)** — A hold-to-talk path that sidesteps wake-word latency and the "hey vee"→"av" misfire class entirely: hold a key (default Space), the mic goes hot and wake-word matching is skipped; release to dispatch. **Opt-in and frontend-only** — enable flag + configurable key (a `KeyboardEvent.code`) live in localStorage via the new `pushToTalk.ts`, default off, so when disabled the wake-word path is byte-for-byte unchanged (PTT state never flips, the transcript intercept is never taken). `voice.ts` gains `finalize()` (flush a FINAL for buffered audio without ending the listen loop); `wakeWord.ts` gains `beginPushToTalk`/`endPushToTalk` that intercept the transcript stream while held/finalizing, accumulate FINAL segments across mid-hold pauses, and dispatch on release (1.2s fallback timer), **without touching the wake `active` flag** (one-shot). `main.ts` binds keydown/keyup on the configured key — guards for auto-repeat, editable targets, and an open Settings panel; `preventDefault` so Space doesn't scroll/activate a focused button; a window-blur safety release; the PTT dispatch skips the echo filter (deliberate hold) but still sends `barge_in` if Vee is mid-reply; `onPushToTalkEnd` reconciles mic state (re-pauses if asleep). Settings toggle + onboarding mention added. No JS test runner exists in the repo (frontend is verified by tsc strict + vite build per `factory.yml`), so the routing is kept pure/thin in one place rather than unit-tested; tsc clean, build OK. Latency note: this removes the wake-phrase recognition + the inter-utterance pause from the front of a turn for power users — a class of latency the t0–t3 instrumentation doesn't even capture (it starts at end-of-speech).
- **2026-06-15 — Speak on the first sentence, not the full reply (perceived-latency PR 1)** — The conversational reply now **streams** (`client.messages.stream`) and TTS starts on each complete sentence as it lands, instead of waiting for the whole reply (`generate_response_streamed`, sharing the extracted `_build_chat_request` so the request is byte-identical to the non-streaming path and the prompt cache behaves the same). Safety leans on `extract_action`: spoken text is always the prefix before `[ACTION`, so a pure `SpeakableStreamer` (new `voice_stream.py`) emits complete sentences only up to the first `[ACTION` marker — action-tag text is never voiced, a literal `[` in speech still is, and exact char-indexing means the unspoken `tail` is voiced once afterward with **no double-speak** (verified by 17 headless + 7 integration tests). **Barge-in:** the audible cut-in is frontend-driven (`audioPlayer.stop()`, untouched) so it's preserved; a `should_cancel` seam is polled between sentences. Genuinely cancelling the reply *server-side* mid-stream is **explicitly deferred** — the conversational reply is awaited inline in the receive loop (so `barge_in` isn't read until it returns; `_cancel_response` is currently vestigial), and making it a cancellable background task is a larger change to the most sensitive path; today's reply is already not server-cancellable mid-flight, so this PR doesn't regress that. **Measured win:** streaming removes a median ~178ms (range ~46–354ms) of model-side wait before first audio on 2-sentence replies, ~0 on single-sentence (no trailing-space boundary → spoken via the tail, matching old behavior). Bounded by VALET's deliberately short (1–2 sentence) replies; the larger levers remain TTS (PR 3) and the model call (PR 4). Scope: main chat path only — work-mode casual stays non-streaming.
- **2026-06-15 — Instrument the voice turn before optimizing it (perceived-latency PR 0)** — Before touching anything that moves latency, the turn is now timed at four marks — t0 end-of-speech, t1 request-to-model, t2 first model token, t3 first audio dispatched — via a pure, headless-tested `voice_timing.TurnTimer` threaded through `generate_response` and `_speak_chunks` behind optional `timer=` params (no behavior change; AppleScript/UC paths untouched). Numbers are always logged (`[voice-timing] …`) and only mirrored to the Process Panel when `VALET_VOICE_TIMING` is set, so default UX is unchanged; the timer holds only mark names + floats, so a summary can't leak transcript content. A headless `scripts/voice_latency_baseline.py` reproduces the two dominant server-side segments without the app. **Baseline (representative, micro-bench against the real Haiku + production TTS proxy, 5-iter):** model t1→t2 ≈ **1.1s** (0.95–1.6s, occasional multi-second spikes), first-sentence TTS t2→t3 ≈ **1.2s** (1.0–2.4s), server-side **t1→t3 ≈ 2.3s** — plus local t0→t1 and the frontend network/decode tail (captured in-app via the log line + browser `[voice-timing]` console line). Confirms conversational turns are on **Haiku 4.5** (`claude-haiku-4-5-20251001`), non-streaming — so TTS can't start until the full reply returns, which is exactly what PR 1 (speak-on-first-sentence) targets.
- **2026-06-12 — Stacked PRs must retarget `main` before merge, or only the base of the stack lands** (UC2–UC6 merge) — The UC2–UC6 PRs (#82–#86) each targeted their parent branch in the stack, so merging them moved code only between feature branches — only UC1 (#80, the one PR opened against main) actually reached `main`. The recovery: one PR (#87, `feat/uc5-barge-in` → main) carrying the full superset, with the 6 conflicts resolved by **taking the superset branch** (it already contained every prior stage) while preserving the independently-merged #81 wake-av fix; verified by 39 passing headless tests + a clean tsc. Lesson recorded so the next stack retargets `main` (or merges bottom-up into main) rather than only into its parent.
- **2026-06-12 — Skills are a free/paid registry over the existing fast-paths; voice terminal is content-tiered** (UC6) — Rather than rebuild the fast-paths, `skills.py` names them and tags each FREE/PAID, with a single `gate()` seam a paywall reads later (no enforcement now). Voice terminal commands are tiered by COMMAND CONTENT, not just capability: a read-only allowlist (git status, ls, version flags) runs at Tier 0; anything else — and anything with shell metacharacters — confirms at Tier 1, with a loud warning on destructive patterns. The "run" verb only fires on a known-tool first word so ordinary speech ("run the tests") stays conversation. Cursor goto uses the `cursor://` deep link; Cursor symbol-search is driven through the UC1/UC3 universal layer (⌘T + type), proving the layer pays off.
- **2026-06-12 — Barge-in keeps the mic live during TTS and filters echo, rather than push-to-talk** (UC5) — The blocker to talking over Vee was the frontend pausing the mic during thinking/speaking; now it stays live and a pure `shouldBargeIn` policy separates a real interruption from Vee hearing its own voice (explicit stop-words always cut in; high word-overlap with the current TTS is treated as echo). A barge-in cancels the in-flight reply AND the running loop **without latching the kill switch**, so the next utterance runs immediately — interrupt is a redirect, not a hard stop. The kill switch stays as the explicit, latching "halt everything". Echo robustness is mic/speaker-dependent and tuned live.
- **2026-06-12 — The observe→act loop forces a tool-call decision and defaults to Sonnet** (UC4) — Each step the model must call a `next_action` tool (typed enum action + ref/text/app/combo), so the decision is always schema-valid — free-text JSON parsing was flaky and stalled the loop. The decide step uses Sonnet, not Haiku: driving a UI step-after-step is agentic reasoning, and Haiku repeatedly mis-picked elements/actions. Supervision is non-negotiable: per-step confirm card + always-checked kill switch + hard step cap + bail-after-2-failures, every beat streamed to the process panel.
- **2026-06-12 — Synthetic input goes to the focused app — the loop confirms each step rather than assuming focus** (UC4) — Live testing surfaced that keystrokes land in whatever app holds keyboard focus (a stray terminal caught them in a CLI harness). The mitigation is the per-step confirm card (the user sees each action before it fires) plus activating the target app before typing; hardening app-focus acquisition before synthetic input is a tracked follow-up. We did NOT paper over it by routing keystrokes through a different backend (that chases the wrong cause).
- **2026-06-12 — NL targets resolve AX-first, vision-fallback, and fail honestly** (UC3) — "Click on Submit" maps to a real element `ref` the model picks from the AX snapshot (validated against the actual list, never invented); only when the AX tree is thin or misses does vision return pixel coords (mapped to screen via the window frame). Ambiguous → ask; absent → "I don't see a 'Submit', sir" — never a wild click. Every resolved action still runs through the confirm card + kill switch and streams the chosen target to the process panel before acting; the resolver itself only decides. For 'type', the field is focused via benign AX so a single confirm covers the keystroke (no double-prompt).
- **2026-06-12 — An "observation" is a focused-window screenshot + the AX snapshot, fused** (UC2) — "What's on screen" no longer captures the whole display; `perception.build_observation` captures only the focused window (`CGWindowList` → `screencapture -l`), downscales to 1366px, and pairs it with the UC1 `observe_ui` element list (numbered refs). The model gets both the picture and the real, clickable elements — so UC3's "click on Submit" resolves against actual `ref`s, not pixel guesses. Screenshot bytes are an in-memory observation for the model only: never written to disk, never logged as an analytics payload (`/api/perception/observe` returns image metadata only; the vision call keeps `capture_input=False`).
- **2026-06-12 — Screen Recording plumbed with a real `CGPreflightScreenCaptureAccess` check** (UC2) — Same pattern as UC1's Accessibility: live TCC status in `/api/permissions/status` (revoke → red), native prompt in `/api/permissions/trigger`, Settings + onboarding inline-enable. No new dependency — the capture + preflight APIs are in the Quartz wheel UC1 already added.
- **2026-06-12 — Universal control is built behind the existing `ActionExecutor` seam, AX-first** (UC1) — The new perception→action layer adds `Capability.{OBSERVE_UI, CLICK_ELEMENT, KEY_COMBO}` as concrete-default `not_supported` methods on the ABC, so the AppleScript backend inherits them untouched; only the Accessibility backend implements them, and the composite routes to it on `not_supported`. The AX tree (`AXUIElement`) is the primary locator (real frames, AXPress); a synthetic `CGEvent` mouse click is the fallback when an element has no press action. This is the "Accessibility/vision backend is a swap, not a rewrite" note from Stage C cashed in.
- **2026-06-12 — A synthesized click or keypress is Tier 1; reading the screen is Tier 0** (UC1) — `OBSERVE_UI` runs straight through (pure observation, still kill-switch-aware); `CLICK_ELEMENT` and `KEY_COMBO` go through the confirm card + kill switch exactly like delete/keystroke, because pressing a button or firing a shortcut mutates the world. New capabilities are added to `safety.classify` at the same time they're added to the enum, so a new control path can never sidestep the gate.
- **2026-06-12 — The Accessibility backend is always composed, gated by a real `AXIsProcessTrusted` check, never a hardcoded null** (UC1) — Replaced the off-by-default `VALET_UI_FALLBACK` env gate with an always-on `safe(composite(applescript->accessibility))`; the backend is inert without pyobjc + the grant, and AppleScript still owns every capability it already handled. `/api/permissions/status` reports the live `AXIsProcessTrusted` bool for Accessibility (a revoke flips it red) instead of the previous hardcoded `null`/"post-v1" — the same silent-no-op anti-pattern as the mic-green bug. The primitives are reachable for testing/UC4 via gated `/api/ax/*` endpoints (through `SafeExecutor`, so click/key still confirm).
- **2026-06-12 — Resolve email recipients from real contacts; ask, never fabricate** — "email Nick" was inventing `Nick@sbsrollover.com` (wrong domain). DRAFT_EMAIL now resolves bare names via the saved-contacts store then Apple Contacts (`CNContactStore`, new macOS permission), and explicitly ASKS the user when the name is unknown rather than guessing. A system-prompt rule forbids inventing addresses. New `SAVE_CONTACT` action persists corrections.
- **2026-06-12 — Persist app data in Application Support, never the PyInstaller temp dir** — The shipped app's `valet.db` (bio/tasks/contacts) used a `__file__`-relative path = a read-only per-launch temp dir, so nothing persisted across launches. All writable app state (SQLite, Google tokens, tracker DB) now resolves to `~/Library/Application Support/VALET/` via `valet_data_dir()`. The same class of bug previously hid in `google_auth`; this closes it for `memory.py` too.
- **2026-06-12 — Merge + de-dup Apple and Google calendar reads** — Google Calendar syncs into macOS Calendar, so reading both naively double-counts events. Calendar reads now merge Apple (EventKit) + Google and drop duplicates keyed on normalized title + UTC minute (date for all-day). Google integration goes live by bundling `google_credentials.json` and writing tokens to the writable data dir, not the ephemeral temp dir.
- **2026-06-12 — "open <website>" opens the URL, never System Events keystrokes** — Opening a site via synthetic keystrokes failed (`-1743/1002`, Accessibility) and silently no-op'd. A `_WEB_DESTINATIONS` map + fast-path now route "open my google calendar"/"open Google Analytics" straight to `open -a` with a URL — no keystrokes, no Accessibility dependency.
- **2026-06-12 — "open" vs "read" a note are different intents** — "open/go to my <X> note" shows it in Notes.app; "read" reads it aloud. Separated so a user asking to see a note doesn't get it dictated. Notes title matching also made robust to unreadable containers (`-1728`) and normalized for case/space/punct.
- **2026-06-12 — Run confirm+dispatch on a background task** — The WS receive loop awaited the confirmation future inline, so it couldn't read the very `confirm_response` it was blocked on — calendar create/cancel + send hung until the 120s timeout. Confirm+dispatch now runs on a separate task (`_confirm_and_dispatch`) so the receive loop stays free to deliver the reply.
- **2026-06-11 — Fair-use cap is now per-plan, but enforcement stays OFF until explicitly enabled** — Shipped the per-plan allowance mechanism (`FAIR_USE_USD_PRO`/`FAIR_USE_USD_ULTRA`, #59) so the shared-key risk is finally closable, but left `FAIR_USE_MODE=warn` (never blocks) so a misconfigured limit can't break paying customers mid-launch. Flipping to `throttle`/`block` is a deliberate, owner-owned step.
- **2026-06-11 — Customers see usage as a percentage, never dollars or the limit** — The customer dashboard exposes only a % (computed server-side so raw cost/limit never reach the browser, #60). Owner admin keeps $/MRR. Avoids anchoring customers on our cost basis or the exact ceiling.
- **2026-06-11 — Duplicate Dock icon was an environment artifact, not a code bug** — Diagnosed the user's two-Dock-icons report as 8+ stale/mounted VALET DMG volumes plus a trashed copy polluting Launch Services, fixed by ejecting + rebuilding the LS database. The backend-faceless change (#62) is good hygiene but was kept separately on its own merits, not as "the fix".
- **2026-06-11 — "open notes/calendar/email did nothing" is intent routing, not the build** — "open X on my computer" hit the project resolver (fixed in #65 by stripping locational suffixes). "open my calendar"/"open email" route to the READ lookups (show schedule/inbox) rather than launching the app — left as an OPEN design decision rather than silently changing it.
- **2026-06-11 — Use `+alias` emails for test purchases** — A pile of test trials had all auto-claimed onto the owner's real email; cleaned up (Stripe customers + `licenses` rows + auth user deleted so trials don't convert to charges). Future testing uses `+alias` addresses so test data is isolated.
- **2026-06-11 — Device-settings ("manage VALET from the browser") ships in 4 phases, web wins on conflict** — Phase 1 (#66) is storage + endpoints only; Phases 2–4 add the editable `/account` card, `server.py` fetch/poll/apply, removal of Proxy URL + Voice ID from the app, and conflict resolution. Web is the source of truth on conflict. Sequenced so each phase is independently shippable.
- **2026-06-11 — Stripe flipped to LIVE under a new "Twin Peaks Labs" account** — Moved off sandbox to live keys, a live webhook on the www host, recreated Pro/Ultra products with new live price IDs, and a saved Customer Portal config. Verified `cs_live_` checkout for both tiers end to end. Payouts deliberately left paused until a bank account is added (activation incomplete); live charges accrue in the balance meanwhile.
- **2026-06-11 — Identity = Supabase Auth accounts, license auto-claimed by buyer email** — Added real email/password login rather than keeping license-key-in-a-header as the only identity. New accounts auto-link their license by matching the Stripe buyer email; a manual link-key form covers mismatches. Email verification is enforced for the web session, but the license keeps working in the app regardless so confirmation friction never blocks usage.
- **2026-06-11 — Desktop→web sync is app-pushes-snapshot, sanitized, telemetry-gated, never message content** — The app pushes a profile/stats/connections snapshot to an authed ingest endpoint every 15 min; the server applies a strict allow-list sanitizer (known fields only, types coerced, extras dropped). Gated on a license key present AND telemetry not opted out. No message content ever leaves the machine. Web-side profile is read-only (synced down); editing-on-web that writes back to the app is deferred.
- **2026-06-11 — Owner admin view is read-only, env allow-list gated** — `/account/admin` is gated on `ADMIN_EMAILS` checked against the verified session email; it only reads (summary cards + subscriber table) with no per-customer actions yet. Keeps the blast radius minimal while giving the owner visibility.
- **2026-06-11 — `NEXT_PUBLIC_SUPABASE_URL` must be the bare project URL** — A `/rest/v1/` suffix produced a PostgREST "Invalid path specified in request URL" error at signup. Fixed to the bare `https://ufqvgujnphaejewqmugg.supabase.co`. Noted so it isn't reintroduced.
- **2026-06-11 — Distribute the signed DMG as a public GitHub release, gated by the proxy** — The notarized `VALET_0.1.0_aarch64.dmg` lives as a release on `Kuba-Ventures/valet-downloads`; `product-site` `/api/download` validates the license then redirects to it via the Vercel `DOWNLOAD_URL` env var. Swapping a future build is one env change. Rejected: serving the binary through Vercel directly (size, bandwidth).
- **2026-06-11 — Onboarding re-runs on every fresh install, not just every build** — Keyed off the `.app` creation-time stamp (PR #44) so a re-download of the same build still re-triggers setup, ensuring permissions/license are re-walked on a clean machine.
- **2026-06-11 — Action analytics log tags only, never raw conversation** — The proxy extracts `[ACTION:TYPE]` tags into Langfuse as trace tags + metadata and drops sensitive targets (file paths on deletes/builds), keeping only app/project names. Gives a "most-used actions" view without storing prompts/responses (`PROXY_CAPTURE_PAYLOADS` off by default).
- **2026-06-11 — PyInstaller `--clean` on every build** — An incremental build had shipped a stale `server.py` + frontend (the wizard-less app). `--clean` wipes the cache every time so a release can never bundle stale code, at the cost of slower builds.
- **2026-06-10 — Stage F packaging will use Tauri, not Electron** — Tauri for a lighter footprint; PyInstaller bundles the FastAPI backend, and the built frontend ships inside the same signed, notarized macOS app.
- **2026-06-10 — Self-editing cut from shipped builds** — `self_mod` is dev-only. Access routes through `_load_self_mod()`, which returns `None` when shipped (`VALET_SHIPPED` or no `.git`), so `self_mod.py` is excludable from the bundle. (`restart_self` still lives in `self_mod` and must be relocated for packaged restart — Stage F.)
- **2026-06-10 — Two British voices, persona unchanged** — Only the Fish TTS model swaps (Male / Female); the butler persona ("Vee") is untouched. `_active_voice_id()` reads live so a settings change applies on the next reply.
- **2026-06-10 — Risk-tiered safety with confirm-first deletes** — Tier 0 (reads/lists/opens/navigate) runs automatically; Tier 1 (delete/move/overwrite, drive apps, keystrokes, scripts, bulk, protected paths) confirms first. Deletes go to Trash, never unlink. Always-available kill switch. New-file write is Tier 0; overwrite is Tier 1.
- **2026-06-10 — Control on a portable ActionExecutor; AppleScript-only for v1** — `ActionExecutor` ABC + `Capability` enum with a macOS AppleScript backend. Non-scriptable apps return a structured `not_supported` (detected via `.sdef`/Info.plist without launching). A Windows/Linux or Accessibility/vision backend becomes a swap, not a rewrite.
- **2026-06-10 — App speaks the native Anthropic Messages dialect** — Repointing to the proxy is a base-URL + header swap (SDK `base_url` → `/api/proxy`, license in `X-License-Key`). Avoids a bespoke proxy protocol and keeps the SDK intact.
- **2026-06-10 — Soft-warn fair use + 7-day offline grace** — The fair-use cap (default $8/mo) warns but never blocks at launch; the license check tolerates a 7-day offline grace window so transient network failure doesn't brick the app. Both are deliberately lenient for launch and must tighten before scale.
- **2026-06-10 — Bundled + metered AI, no user API key** — The product bills centrally through the hosted proxy; the download ships with no vendor secrets. Rejected: making each user supply their own Anthropic/Fish key (worse UX, no metering, no entitlement control).
- **2026-06-10 — Proxy lives in product-site (one Vercel deploy)** — The license-gated proxy is co-located with the marketing/checkout site rather than a separate service, so there's a single Vercel deploy and one place the vendor keys live.
- **2026-06-10 — Leave the voice-dispatch core in server.py** — Stage B2 extracted only cleanly-decoupled concerns (`task_manager.py`, `voice_text.py`, `project_scanner.py`). Splitting `voice_handler` + the `_execute_*` handlers would be a fake boundary with high regression risk for no gain.
- **2026-06-10 — Retire JARVIS / Marvel branding → VALET (Vee)** — Renamed the assistant everywhere in the running product (persona, wake words, logger namespaces `valet.*`, launchd label `com.valet.backend`, log filenames, marketing site, README). Preserved deliberately: the `jarvis-y` GitHub remote/Vercel URL and historical docs (`OVERNIGHT_*.md`, `docs/*_diagnosis.md`).
- **2026-06-10 — Supervised PR factory before the major-changes wave** — Two required checks, fail-closed reviewer, branch protection; auto-merge OFF (`FACTORY_AUTOMERGE` unset) until a ~2-week soak.
- **2026-06-04 — Target Cursor's terminal by AXDescription, not AXRole** — The focused terminal element is an `AXTextField` whose AXDescription contains "terminal"; AXRole can't discriminate it from the palette/editor.
- **2026-06-04 — Verify the Claude pane is live before pasting, else file-ship** — Classify each Cursor terminal pty by its foreground `ps` process; only paste into a confirmed live `claude` pane, otherwise fall back to a staged file ship.
- **2026-06-03 — Hard-cap warm context at 6000 chars** — An oversized (~44KB) clipboard paste raced the Enter key and never landed; `project_context.py` now truncates composed context with a visible marker.
- **2026-05-22 — Dedicated weather pipeline instead of generic research** — `[ACTION:CHECK_WEATHER]` + Open-Meteo (free, structured, no API key) replaced routing through `[ACTION:RESEARCH]`.
- **2026-05-21 — Greenfield projects scaffold before first prompt** — `new_cursor_project` runs an initial commit so the clean-tree gate doesn't block the first ship.
- **2026-05-21 — Strip em-dashes from LLM output at runtime** — Em-dashes were an LLM tell; runtime responses strip them. (Persona rule: hard no-em-dash.)
- **2026-05-20 — Design-mode requires explicit opt-in** — Design mode requires an explicit phrase rather than inferring from intent.
- **2026-05-17 — Native research via Opus web tools** — Replaced folder-based research output with native Opus web tools and streaming source-preview cards.
- **Architectural — Mail is read-only by design** — Sending mail is intentionally out of scope.
- **Architectural — AppleScript over OAuth** — Calendar/Mail/Notes use native AppleScript; no token management. Google Calendar is the one OAuth exception.

---

## Open loops  [rewrite]

- [ ] **Land Stage 3 (#139)** — guided walkthroughs (`walkthrough.py`) + menu-bar UI polish; open PR, on-device validation via the signed debug bundle pending — owner: Finley
- [ ] **Stage 4 — cut a signed/notarized DMG carrying the whole menu-bar product + publish (top shipping gate)** — menu-bar shell + ⌃⌥ chord + voice-native Raycast + walkthroughs + native overlay + voice-console/cursor-glide/PTT foundation + account login + UC1–UC3, behind Accessibility + Screen Recording + Input Monitoring grants, then point `DOWNLOAD_URL` at it — owner: Finley
- [ ] **Wire Input Monitoring into onboarding/Permissions** — the ⌃⌥ chord needs the macOS Input Monitoring grant (stable now it's in the signed main process); request + surface it like Accessibility / Screen Recording. Part of Stage 4 — owner: Finley
- [ ] **Enable branch protection "require branches up to date before merging"** — two squash-merges silently dropped commits in an earlier session (#114 dropped #113's `server.py`; #116's nested-bundle fix dropped), restored via #115 — owner: Finley
- [ ] **Rotate the exposed AssemblyAI eval key** — leaked in a Clicky-benchmarking transcript; likely already tracked, confirm rotated — owner: Finley
- [ ] **Raycast console v3 (incremental)** — Stage 2 (#137/#138) shipped the core (file/settings/system-action search); remaining: calculator, snippets, clipboard history, window management — owner: Finley
- [ ] **/admin usage analytics dashboard (backlog)** — `coding-plans/backlog.md`; needs a VALET-usage-vs-OS-surveillance privacy decision + a local→Supabase telemetry pipeline first — owner: Finley
- [ ] **Deploy account-login PR A (#97) to Vercel** — the app side (#98–#102) is on main but `/api/account/login` forwards to the proxy's `/api/account/app-login`; account login only works end-to-end after a `product-site` redeploy — owner: Finley
- [ ] **Web contacts editor** — the in-app Contacts editor was removed (#102) in favor of a "manage in browser" link; build the web-side contacts editor on `/account` — owner: Finley
- [ ] **Synthetic-input target-focus hardening (UC4)** — keystrokes go to whatever app holds keyboard focus (a terminal caught them in CLI tests); needs robust app-focusing before typing. Blocks shipping UC4/UC6-terminal — owner: Finley
- [ ] **Live in-app passes (running/signed build)** — UC4 confirm-card/STOP click-through of the observe→act loop, Screen-Recording grant/revoke UI, UC5 barge-in echo tuning on real hardware — owner: Finley
- [ ] **Keep STOP visible during the UC4 loop** — the STOP button is hidden when idle (`main.ts`); the loop must set the "active" state so STOP stays visible throughout — owner: Finley
- [ ] **Turn ON fair-use enforcement** — mechanism shipped (#59); set `FAIR_USE_MODE=throttle` + `FAIR_USE_USD_PRO`/`FAIR_USE_USD_ULTRA` in Vercel to close the top shared-key risk — owner: Finley
- [ ] **Device-settings Phase 4** — Phases 1–3 landed (#66/#68/#69); remaining: conflict resolution (web wins) + poll cadence — owner: Finley
- [ ] **Fix mic-green permission detection (backend)** — `/api/permissions/status` returns null for Microphone; needs a real mic/speech TCC check via bundled pyobjc (affects onboarding + Console Permissions) — owner: Finley
- [ ] **Design decision: "open my calendar"/"open email" — launch app or show data?** — currently route to READ lookups; open call — owner: Finley
- [ ] **Add a bank account in Stripe to resume payouts** — live charges accrue but cannot pay out until account activation is complete — owner: client
- [ ] **Calendar cancel/delete on the Google path** — the Apple/EventKit path is done; verify cancel/delete works (or is routed) on the Google calendar path — owner: Finley
- [ ] Add a TTS fallback so a Fish Audio outage doesn't kill voice — owner: Finley
- [ ] Build the planned Langfuse usage dashboard (MCP connected) — owner: Finley
- [ ] Decide the exact fair-use number (currently $8/mo soft) — owner: Finley
- [ ] **Relocate `restart_self` out of `self_mod`** so packaged restart works after self_mod is excluded — owner: Finley
- [ ] Add `CLAUDE_CODE_OAUTH_TOKEN` secret to `jarvis-y` so factory-review can authenticate — owner: Finley
- [ ] After the ~2-week factory soak, decide whether to flip `FACTORY_AUTOMERGE` on — owner: Finley

---

## Risks & known issues  [rewrite]

- **The whole menu-bar product is DEV/DEBUG-BUNDLE ONLY — nothing reaches customers until Stage 4 cuts a release build.** The menu-bar shell + ⌃⌥ chord PTT (#135/#136), voice-native Raycast (#137/#138), walkthroughs (#139), the native cursor follower (#126), the web-click fix (#128), the voice Mac-control foundation (#113–#123), the account-login + perceived-latency tracks, and the whole UC0–UC6 track are all on `main` (Stage 3 in #139) but in NO signed/notarized /Applications DMG. The demo does not "ship" until the DMG is rebuilt, re-signed, notarized, and `DOWNLOAD_URL` is repointed. This is the single outstanding gate before the sponsor demo is real.
- **The ⌃⌥ chord needs Input Monitoring, not yet wired into onboarding.** The tap now runs in the signed main process (so the grant is stable across rebuilds, unlike the retired backend-helper tap), but onboarding/Permissions doesn't request or surface Input Monitoring yet — a fresh install will have a silent no-op chord until the user grants it manually. Part of Stage 4.
- **The native overlay's click-through is the eng-review's flagged gap.** `set_ignore_cursor_events(true)` must hold or the transparent overlay eats every click. Runs only via `tauri dev` / the debug bundle today.
- **The merge train silently dropped commits twice this session.** Squash-merging branches that were behind their base dropped a parent's commits not present in the squashed diff: #114 dropped #113's `server.py` (restored by #115) and #116's nested-bundle fix was dropped and restored. Until "require branches up to date before merging" is enabled, this can recur and quietly regress code that tests don't cover.
- **An AssemblyAI eval key was exposed in a transcript** (from the Clicky benchmarking). Rotate it; likely already tracked.
- **TOP RISK (now ADDRESSABLE, not yet closed) — shipped users bill the owner's personal keys with the cap in warn mode.** Every shipped user's AI + TTS runs through the proxy's single owner-held Anthropic + Fish keys. The per-plan fair-use mechanism now exists (#59) but `FAIR_USE_MODE` still defaults to `warn`, so it never blocks. With a signed build live, anyone with a license can drive unbounded spend until the owner sets `FAIR_USE_MODE=throttle`/`block` + the per-plan allowance envs in Vercel. **This is the single concrete step to close the risk** (see "What's left", item 2).
- **Mic-green permission detection is broken (backend).** `/api/permissions/status` returns null for Microphone, so the green indicator never lights even when mic is granted — in both onboarding and the new Console Permissions section. Needs a real mic/speech TCC authorization check via the bundled pyobjc.
- **The Universal Control track is on main but NOT in any signed build.** UC0–UC6 are all merged, but no signed/notarized DMG has been packaged with them, so downloaders have none of it yet. Shipping it also gates on open caveats: synthetic-input target-focus hardening (keystrokes follow keyboard focus), the live in-app passes (UC4 confirm/STOP, Screen-Recording grant/revoke UI, UC5 echo tuning), and the STOP-visible-during-the-loop UI fix. Ship UC1–UC3 first; hold UC4/UC6-terminal/UC5 until those clear.
- **Synthetic input follows keyboard focus (UC4).** Keystrokes land in whatever app holds focus — a stray terminal caught them in a CLI harness. Mitigated in the real app by per-step confirm + activating the target app, but robust app-focusing before typing is unbuilt. This is the headline reason UC4/UC6-terminal are held from the first signed build.
- **Stripe payouts are PAUSED.** Live mode is on and verified, but account activation is incomplete — live charges accrue in the Stripe balance and cannot pay out until the client adds a bank account.
- **Single-vendor TTS** — Fish Audio has no fallback; an outage takes voice down.
- **Soft fair-use + 7-day offline grace are intentionally lenient.** A canceled/abused license keeps working for up to 7 days offline; over-allowance never stops a request. Fine for a controlled launch, not for scale.
- **`restart_self` still lives in `self_mod`**, which is excluded from shipped builds — packaged restart relies on the watchdog relaunch; the in-app `restart_self` path stays broken until relocated.
- **Factory not yet functional end-to-end** — factory-review needs `CLAUDE_CODE_OAUTH_TOKEN` (repo has zero secrets) before it can pass.
- **Self-signed certs** require manual Chrome trust on first run; documented only as the openssl command.
- **Anthropic 529s** during the design turn previously forced an emergency model swap; no resilience layer added since.

---

## Roadmap / Deferred  [append-only — intentionally deferred for future builds]

*As of 2026-06-11. This captures what is deliberately put off so it survives across sessions.*

**Rides the NEXT signed app build (needs rebuild + re-sign + notarize):**
- **#44 (merged): re-run onboarding on EVERY install, not just every new build.** On `main`, NOT in the build currently being deployed.
- The "sir" persona pass (below) batches here too.

**Merged, awaiting config to take effect:**
- **License-key email on purchase (#46, merged).** The Stripe webhook emails the buyer their key. Graceful no-op until activated. Activation: create a Resend account, verify a sending DOMAIN you own (cannot verify `*.vercel.app`, so a real domain is needed, e.g. `valetvoice.app`), then set `RESEND_API_KEY` and `EMAIL_FROM` in Vercel and redeploy.

**Config still owned by the operator:**
- *(2026-06-11) DONE: Stripe is now LIVE.* Remaining: add a bank account to resume payouts (client's responsibility).
- *(2026-06-11) DONE: transactional email is LIVE* — Resend domain verified, `RESEND_API_KEY`/`EMAIL_FROM` set, Supabase custom SMTP wired, www `/auth/callback` redirect added.
- **OPEN: turn ON fair-use enforcement** — set `FAIR_USE_MODE=throttle` + `FAIR_USE_USD_PRO`/`FAIR_USE_USD_ULTRA` in Vercel. Mechanism shipped (#59); flipping it on is the step that actually closes the shared-key risk.

**Voice Mac-control roadmap (post-2026-06-18 push):**
- ~~**Native cursor follower + global hold-PTT = the native-overlay spike**~~ — *LARGELY SHIPPED (dev/debug-bundle): eng-review locked the architecture (#125), the cursor follower landed (#126) and went multi-monitor (#136), and global PTT became the in-main-process **⌃⌥ chord** (#135/#136) — superseding both the planned `tauri-plugin-global-shortcut` chord and the backend `global_ptt.py` CGEventTap. Remaining: show-dot-only-while-steering, Input Monitoring in onboarding, and shipping it in the Stage-4 DMG.*
- ~~**Raycast console (no-LLM file/settings/system-action search)**~~ — *SHIPPED (dev/debug-bundle) as menu-bar Stage 2 (#137/#138): `file_index.py` / `settings_index.py` / `system_actions.py`. v3 nice-to-haves remain (calculator, snippets, clipboard, window management).*
- ~~**Guided walkthroughs (teach, don't do)**~~ — *BUILT as menu-bar Stage 3 (#139, open): `walkthrough.py` glides the cursor to each step and waits for the user.*
- **/admin usage analytics dashboard** (`coding-plans/backlog.md`) — needs a "VALET-usage telemetry vs. OS-surveillance" privacy decision and a local→Supabase telemetry pipeline before building.

**Deferred features (not started):**
- ~~**Google Calendar / Gmail**~~ — *SHIPPED 2026-06-12 (691b613): per-user OAuth wired in the frozen app (creds bundled, token persists), Gmail drafts + Google Calendar reads merged/de-duped with Apple. Remaining: calendar cancel/delete on the Google path; Google app verification for non-owner accounts at scale.*
- ~~**Apple Contacts / recipient resolution**~~ — *SHIPPED 2026-06-12 (981e70e): saved-contacts store + Apple Contacts fallback behind a new Contacts permission; "email Nick" resolves a real address or asks. Was part of the deferred "Google + Gmail / contacts" bucket.*
- ~~**User accounts / self-serve billing portal**~~ — *BUILT: Supabase-Auth accounts, `/account` dashboard (key, plan, %-usage, download, Stripe Billing Portal), owner `/account/admin`, desktop→web sync (PR #57).* Device-settings Phases 1–3 landed (#66/#68/#69); only Phase 4 (conflict resolution + poll cadence) remains.
- ~~**Manage VALET from the browser (device-settings)**~~ — *Phases 1–3 MERGED: Phase 1 store + endpoints (#66), Phase 2 editable `/account` card (#68), Phase 3 app applies web settings at launch + Proxy URL/Voice ID removed from app (#69). Only Phase 4 (conflict resolution, web wins; poll cadence) remains. See "What's left".*
- **"Sir" persona pass:** roughly 20 instances in the app system prompt instruct Vee to address the user as "sir" (the old assistant persona). A deliberate tone decision plus an app rebuild.
- **TTS fallback:** Fish Audio is the single vendor with no backup. Add a secondary provider path.
- ~~**Universal app control via the macOS Accessibility API (AXUIElement)**~~ — *BUILT + MERGED 2026-06-12 (Universal Control track UC0–UC6, on main): AX-tree read + synthetic input, screenshot + vision fallback for thin/Electron trees, every click/type gated by the existing confirm + kill switch. Remaining: cut a signed build with it, the synthetic-input focus hardening, and the live in-app passes. See "What's built → Stage K / UC1–UC6" and "What's left".*
- **Langfuse usage dashboard:** the Langfuse MCP is connected; build the apps-opened / actions-done / friction widgets (needs live action-tag data flowing first).

**Top liability status:** the per-plan cap *mechanism* shipped (#59), so shared-key spend is now BOUNDABLE — but enforcement is **not yet on** (`FAIR_USE_MODE` defaults to `warn`). It becomes bounded only once the owner sets `FAIR_USE_MODE=throttle` + `FAIR_USE_USD_PRO`/`FAIR_USE_USD_ULTRA` in Vercel. *(Corrects the earlier note that claimed this was already enabled — it was not.)*

---

## Links  [rewrite]

- **Live URL (proxy + marketing + accounts):** https://www.valet-voice.com (Vercel; `jarvis-y.vercel.app` is the underlying deploy)
- **Customer account portal:** https://valet-voice.com/account (owner admin at `/account/admin`)
- **App:** local-only (`http://localhost:5173`)
- **GitHub:** https://github.com/Kuba-Ventures/jarvis-y (remote/URL names deliberately NOT renamed; org is Kuba-Ventures; local dir is `~/Code/VALET`)
- **Staging:** n/a
- **Client Drive folder:** n/a
- **Slack channel:** n/a
- **Download (release DMG):** https://github.com/Kuba-Ventures/valet-downloads/releases/tag/v0.1.0 (`VALET_0.1.0_aarch64.dmg`, signed + notarized; Vercel `DOWNLOAD_URL` points here)
- **Related repos:** `Kuba-Ventures/valet-downloads` (public, hosts the signed DMG releases); `product-site/` (the Next.js marketing + proxy site, in this repo); Kuba-Ventures soultech / Dharma (the PR-factory pattern this repo mirrors)

---

## Changelog  [append-only — never rewrite or delete]

- **2026-06-21:** kuba-vault refresh — **VALET restructured into a MENU-BAR PRODUCT** (sponsor mandate, Jacques Arnoux / Twin Peaks weekly), built in four staged steps (`coding-plans/`). Merged to `main`: **#135** Stage 1 — menu-bar shell (`ActivationPolicy::Accessory`, no Dock icon; tray icon/menu; always-on-top draggable orb popover; `prevent_exit`) + a global held **⌃⌥ chord = push-to-talk from any app**; **#136** Stage 1 on-device hardening — moved the ⌃⌥ tap into the MAIN Tauri process (raw `core-graphics` `CGEventTap`, retiring the backend `global_ptt.py` sidecar so Input Monitoring is one stable signed grant), multi-monitor cursor follower, lifecycle/tray-icon fixes, and the key finding that `tauri dev` can't run this app (TCC-gated Web-Speech STT needs a real `.app`) so testing uses a **signed debug `.app` bundle**; **#137** Stage 2 — voice-native Raycast: no-LLM file (`file_index.py`, Spotlight), settings deep-link (`settings_index.py`) + tiered system-action (`system_actions.py`) search; **#138** Stage 2 file-search quality — scope to document folders + broaden "open my X" triggering. **Open:** **#139** Stage 3 — guided walkthroughs (`walkthrough.py`, teach-don't-do: glide the cursor to each control without clicking, narrate, wait + re-observe; "do it for me" is the only gated click) + menu-bar UI polish (drag-from-anywhere, Settings-from-tray, readable purple ⌃⌥ hint, black template tray icon). Also folded in: the earlier-session web-click coverage bump (`target_resolver` 80→200, `accessibility_executor` 250→400) and global PTT are now merged, the latter superseded by the ⌃⌥ chord. Added a "What's built → menu-bar restructure" subsection, tech-stack rows (menu-bar form factor, ⌃⌥ chord, Raycast modules, walkthroughs, multi-monitor overlay), and reworked TL;DR / Status / Where-we-are / What's-left / open loops / risks / roadmap around the four stages. The Decisions log already carried the four dated Stage entries (#135/#136/#137/#139) — left untouched, not duplicated. **Everything is dev/debug-bundle only; Stage 4 (signed/notarized DMG + Input-Monitoring onboarding + `DOWNLOAD_URL`) is the outstanding ship gate. `DOWNLOAD_URL` still unset.** Committed PROJECT.md only.
- **2026-06-20:** kuba-vault refresh — **native + system-wide Mac-control push** (sponsor mandate, Jacques Arnoux / Twin Peaks weekly), folding in everything since #124. `/plan-eng-review` locked the native-overlay architecture (#125: CSS-transform dot in a transparent click-through overlay, NOT per-frame window moves or a Swift sidecar; global PTT chord via `tauri-plugin-global-shortcut` since bare ⌥ isn't registerable). Merged to `main`: **#126** native cursor follower first slice (fullscreen transparent always-on-top click-through Tauri overlay holding a CSS dot, moved ~60fps by a Rust cursor-poll loop via `window.eval()`; needed Tauri `macOSPrivateApi` + the `macos-private-api` Cargo feature; primary-monitor only); **#127** PTT-silence fix (#118+#122 interaction dropped all audio in PTT mode — split a new `muted` hard-stop flag from `isSleeping`, status shows "listening…" while holding ⌥); **#128** web-click AX fix (`observe_ui` sets `AXManualAccessibility`/`AXEnhancedUserInterface` to force Chromium's full tree so web elements resolve by ref, not vision; one-time per pid, cached). **Captured uncommitted dev-test work in the working tree (deliberately not committed):** the web-click element-COVERAGE follow-up (`target_resolver` 80→200, `accessibility_executor._MAX_ELEMENTS` 250→400, build delay 0.35→0.6s — GitHub nav/avatar were past the old limits) and **global push-to-talk** — new `global_ptt.py` Quartz `CGEventTap` (listen-only, daemon thread + own `CFRunLoop`) watching the ⌥ flag system-wide; `server.py` lifespan bridges it via `run_coroutine_threadsafe` to `process_bus.broadcast({"type":"ptt",...})` (new generic `broadcast()` on the bus, `emit()` delegates to it); `main.ts` mirrors the in-window ⌥ path so hold-⌥ works in ANY app (needs macOS Input Monitoring at ship time). Built + running in dev, not yet PR'd. Added a "What's built" subsection, 4 decisions (incl. the global-PTT CGEventTap-vs-Tauri-shortcut architecture call), reordered "What's left" / open loops around the in-progress native overlay + the uncommitted work + the new Input-Monitoring permission loop, updated TL;DR / Status / Where-we-are / risks / the roadmap's native-overlay bullet (now IN PROGRESS). **All still dev-server-only — not in any signed build.** Did NOT stage or commit any of the uncommitted backend/frontend changes; committed PROJECT.md only.
- **2026-06-18/19:** kuba-vault refresh — **voice-native Mac-control push** (sponsor mandate, Jacques Arnoux / Twin Peaks weekly: FAST + intuitive, benchmark Hey Clicky, build on Clicky, deprioritize billing/admin), all merged to `main` (#113–#123). Scoped via `/office-hours`, built in stages (`coding-plans/voice-mac-control-build-plan.md`). Shipped: **#113** point-and-teach ("where is X" speaks the location, structurally input-free; `target_resolver` carries the frame, `emit_pointer`, `_resolve_and_point`); **#114** Stage 0 latency harness (`GET /api/latency/last`); **#116** Stage 1 voice console ("open X" launches installed apps with NO LLM round-trip via new `app_index.py` — nested bundles, space-insensitive); **#117** Stage 2 visible cursor glide + hit-test-guarded click (`move_cursor`, threaded tween, physical-mouse abort, AX hit-test, Escape hard-stop); **#118** Escape kills in-flight voice (`_speak_cancel`); **#119** cursor-takeover banner; **#120** no confirm panel for voice clicks (kill-switch still honored) + faster misses ~5s→~1-2s (vision at most once, AX-only retry); **#121** echo-loop fix (post-speech cooldown); **#122** PTT-primary — hold-⌥ is now the DEFAULT, always-listening is a toggle (supersedes #92); **#123** roadmap/backlog docs. Recorded the correction: Clicky teaches/points but VALET already CLICKS — speed is the product. Added a "What's built" subsection, an STT/app-launch tech-stack update, 7 decisions, a forward-looking voice-Mac-control roadmap block, and reordered "What's left" / open loops around the native-overlay spike. **All dev-server-only — not in any signed build (the outstanding gate before the demo ships).** Two carried risks logged: the merge train silently dropped commits twice (#114 dropped #113's `server.py`; #116 nested-bundle fix — restored via #115) → enable "require branches up to date before merging"; and an AssemblyAI eval key was exposed in a transcript → rotate. Billing/admin (fair-use flip, payouts, device-settings Phase 4, PR A deploy) deliberately deprioritized but unchanged.
- **2026-06-15:** kuba-vault refresh — **account login + perceived-latency tracks** since the last doc update. Account login (#97–#102) ships end-to-end: PR A (#97, product-site) added `getSupabaseAnon()` + `loginAndProvision()` + `/api/account/app-login` + `/api/proxy/profile`; PR B (#98, app) added the login section + `/api/account/login` that writes the provisioned `LICENSE_KEY` + profile to `.env` (password forwarded for verification only, never stored). #100 put the login in the onboarding Activate step (license-key paste as fallback); #99 styled the `[type=email]` field + collapsed the form to "Signed in as <email> · <plan>"; #101 fixed the setup-mode stacking bug that kept asking users to log in after onboarding + surfaced Name · Tier · License #. **#102 is the critical fix:** `_reinit_after_license()` rebuilds the proxy LLM client + `LICENSE_KEY` global right after login so LLM + TTS work in-session — `needs_relaunch` now always false (supersedes #98's "restart to activate"); also removed the onboarding Connections step + in-app Contacts editor (→ "manage in browser" link). Perceived-latency track (#90–#96): instrument the turn (#90), speak on the first sentence via streaming (#91), env-tunable Fish latency knob (#93), pre-warm + keep-alive proxy connection ~139ms/TTS-call (#94), STT-stay-on-Web-Speech investigation (#95), push-to-talk + on-screen hint (#92/#96). Added "What's built" subsections for both tracks, logged 3 new decisions (#99/#101/#102), updated TL;DR / Status / Where-we-are / What's-left / open loops. **Still NOT in any signed build, and account-login PR A not yet deployed to Vercel** — both gate end-to-end production use. UC0–UC6 unchanged on main (no signed build cut). Fair-use enforcement still `warn`; Stripe payouts still paused.
- **2026-06-12:** **Universal Control track (UC0–UC6) merged to `main`.** Reconciled PROJECT.md from "stacked PRs, not on main / STOPPED for review" to **landed**. Merge reality: #80 (UC1: Accessibility executor — enumerate/click/type/keys, real `AXIsProcessTrusted`) and #81 (wake on "av" — recognizer collapses "hey vee") merged to main directly; UC2–UC6 (#82–#86) were stacked PRs that each merged into their parent branch, so only UC1 initially reached main — caught and fixed by **PR #87** (`feat/uc5-barge-in` → main), which landed all of UC2–UC6 in one go (6 conflicts resolved by taking the superset branch, the #81 wake-av fix preserved, 39 headless tests pass, tsc clean). On main now: `accessibility_executor.py` (rewritten), `perception.py`, `target_resolver.py`, `agent_loop.py`, `skills.py`, `terminal_skill.py`, `frontend/src/bargeIn.ts`, `scripts/ax_smoke.py`, `tests/test_uc{1,2,3,4,6}_*.py`. Two new TCC permissions (Accessibility `AXIsProcessTrusted`, Screen Recording `CGPreflightScreenCaptureAccess`) plumbed end-to-end with real checks; `pyobjc-framework-ApplicationServices` added to requirements + spec (found Quartz was in requirements but never installed — the K1 AX stub had been inert). **No signed build cut with any of this yet.** Carried forward prominently: synthetic-input target-focus hardening (keystrokes follow keyboard focus), the live in-app passes (UC4 confirm/STOP, Screen-Recording grant/revoke UI, UC5 echo tuning), the STOP-visible-during-loop UI fix, and the ship proposal (UC1–UC3 first). Logged the stacked-PR-retarget-main lesson as a decision. Marked the doc's UC subsections, track summary, open loops, risks, and the "Accessibility-API universal control" roadmap item as merged/shipped.
- **2026-06-12:** **UC6 — Skills as accelerators (terminal + Cursor)** on branch `feat/uc6-skills-terminal-cursor` (stacked on UC5, not merged) — **closes the Universal Control track (UC0–UC6).** New `skills.py` (free/paid registry + `gate()` seam, `/api/skills`); `terminal_skill.py` voice terminal commands tiered by content (read-only→Tier 0 auto, else→Tier 1 confirm, destructive→warned); Cursor goto (`cursor://` deep link) + symbol-search via the universal layer; `/api/skill/{run,cursor/goto,cursor/symbol}` + voice fast-paths. 9 headless tests (39 total UC1–UC6); live-validated (git status ran at Tier 0, npm install→Tier 1, registry 6 free/12 paid). Added a track summary + signed-build ship proposal to "What's built". Stacked PR; full stack #80→#86, none merged — STOPPED for review.
- **2026-06-12:** **UC5 — Interruptible / barge-in voice** on branch `feat/uc5-barge-in` (stacked on UC4, not merged). The mic now stays live during TTS (was paused) so you can talk over Vee; new `frontend/src/bargeIn.ts` `shouldBargeIn` filters echo (keyword interrupts always cut in; high-overlap-with-TTS = echo). `main.ts` tracks the spoken text + sends a `barge_in` on real interruption; `server.py` `barge_in` cancels the in-flight reply + the tracked UC loop/act task without latching the kill switch (so the next utterance runs immediately), `_track_uc` makes the loop cancellable, and a fresh command also interrupts a running loop. Barge-in logic verified 10/10; frontend builds, backend compiles, 30 UC1–UC4 tests still pass. Live talk-over-TTS timing + echo tuning pending (needs speech). Stacked PR. STOPPED for review before UC6.
- **2026-06-12:** **UC4 — Observe→decide→act loop** on branch `feat/uc4-observe-act-loop` (stacked on UC3, not merged). New `agent_loop.py`: observe (UC2) → model decides next action via a forced `next_action` tool-call (Sonnet) → gated execute (UC1/UC3) → re-observe, with a hard step cap, per-step confirm + kill switch, veto-stop, and bail-after-2-failures; every beat streamed to the process panel. `server.py` `_run_ui_task` + `/api/ui/task` + `[ACTION:UI_TASK]` voice path. 6 headless tests (30 total UC1–UC4); live-validated end-to-end on TextEdit (type→cmd+a→done). Caveat found live: synthetic input follows keyboard focus (a terminal caught the keystrokes in the CLI harness) — mitigated by per-step confirm; robust target-focusing is a follow-up. Stacked PR. STOPPED for review before UC5.
- **2026-06-12:** **UC3 — Click/type by natural language** on branch `feat/uc3-nl-target-resolution` (stacked on UC2, not merged). New `target_resolver.py`: "click on Submit"/"type X in the Y field" → the model picks an element `ref` from the UC2 AX snapshot (AX-first), with a screenshot+vision pixel→screen fallback for thin/Electron UIs; ambiguous→ask, miss→honest fail. `server.py` `_resolve_and_act` + `/api/ui/act` + a voice fast-path (`ui_act`/`_handle_ui_act`) execute through the confirm card + kill switch and stream the chosen target to the process panel; 'type' focuses the field via benign AX (`focus_element`) so it's one confirm. 8 headless tests (24 total UC1–UC3); live-validated on TextEdit (typeface/font-size/text-color resolved to the right refs; "Submit" → honest miss). Stacked PR. STOPPED for review before UC4.
- **2026-06-12:** **UC2 — Screen perception** on branch `feat/uc2-screen-perception` (stacked on UC1, not merged). New `perception.py`: focused-window capture (`CGWindowList` → `screencapture -l`, downscale to 1366px via `sips`) fused with the UC1 AX snapshot into one observation; `describe_observation` sends image + numbered elements to the model via the proxy. Screen Recording permission wired end-to-end (real `CGPreflightScreenCaptureAccess` status, native prompt, Settings + onboarding UI) — no new dependency. `server.py` "what's on screen" path rebuilt on the observation; new read-only `/api/perception/observe` (image metadata only, no raw bytes logged). 7 headless tests pass; live-validated on-device (TextEdit → 42 elements + 1366×1137 PNG; Haiku read the actual on-screen text accurately). Stacked PR opened against the UC1 branch. STOPPED for review before UC3.
- **2026-06-12:** Universal-control track kicked off — UC0 audit (no code) mapped the 9 source-of-truth features to exists/partial/missing against the real code, then **UC1 — Accessibility primitives** on branch `feat/uc1-accessibility-executor` (not merged). Behind the existing `ActionExecutor`/`Capability` seam (additive, AppleScript untouched): new `accessibility_executor.py` with `observe_ui` (AX-tree enumeration → ref/role/title/value/enabled/frame), `click_element` (AXPress-first, `CGEvent` mouse fallback), `key_combo`, plus a real `AXIsProcessTrusted` gate; `Capability.{OBSERVE_UI,CLICK_ELEMENT,KEY_COMBO}` + `UIElement` in `action_executor.py`; safety tiering (observe = Tier 0, click/key = Tier 1) in `safety.py`; composite/safe routing; the AX backend now always composed (`safe(composite(applescript->accessibility))`, replacing the `VALET_UI_FALLBACK` gate); gated `/api/ax/{observe,click,type,key}` endpoints. Accessibility permission wired end-to-end — real status in `/api/permissions/status` (was a hardcoded null), native prompt in `/api/permissions/trigger`, Settings + onboarding UI — and fixed a pre-existing infinite-recursion bug in onboarding `loadPerms()`. Packaging: `pyobjc-framework-ApplicationServices` added to `requirements.txt`, `valet.spec` `collect_all`s Quartz/ApplicationServices/CoreText (found Quartz was never installed in the dev venv, so the K1 AX stub had been inert). 9 headless tests pass (`tests/test_uc1_accessibility.py`); `observe_ui` live-validated on-device (Finder + a 250-element Chrome window); synthetic click/type/key need on-device acceptance in the signed build. Logged three decisions. STOPPED for review before UC2.
- **2026-06-12:** kuba-vault refresh — app-correctness session, 7 commits on branch `fix/eventkit-bundling-status` (ahead of main, not merged), all tested in signed+notarized builds reinstalled to `/Applications`. Shipped: a **contacts system** so "email Nick" resolves a real saved/Apple-Contacts address (new `contacts_access.py`, `memory.py` `contacts` table, `SAVE_CONTACT`, `/api/contacts`, new **Contacts** macOS permission, `pyobjc-framework-Contacts`) instead of fabricating a wrong-domain address (981e70e); **Google OAuth wired** so Gmail + Google Calendar work in the frozen app (bundled `google_credentials.json`, tokens persist, new `google_calendar.py`, Apple+Google calendar reads merged + de-duped) (691b613) — closes the long-deferred "Google + Gmail" item; "open <website>" opens the URL directly with no keystrokes (8d678fe); "open/go to my <X> note" shows it in Notes.app (b3950a4); notes fuzzy-match robust to unreadable containers + honest "Gmail not connected" (0fac61b, c6c9717); **confirm-flow deadlock** fixed (calendar create/cancel + send no longer hang 120s) (1e3a028). Also fixed a **latent data-loss bug**: the shipped `valet.db` wrote to the read-only PyInstaller temp dir, so bio/tasks/contacts never persisted across launches — now writes to `~/Library/Application Support/VALET/`. Logged six decisions; updated tech-stack (EventKit/CNContactStore), integrations (Apple Contacts + Google live), open loops (publish DMG + merge branch added; Google/Gmail + onboarding-connections loops closed), risks (Google-deferred + unsigned-build risks replaced with "fixes not yet on published DMG"), and the Roadmap (Google/Gmail + Apple Contacts struck as SHIPPED). The notarized `VALET_0.1.0_aarch64.dmg` is built + stapled but NOT published (`DOWNLOAD_URL` unchanged) — pending merge to main.
- **2026-06-11 (late eve):** kuba-vault refresh + handoff — Finley stepping away, added a prominent "What's left to pick up" section. Since the account-portal/live-Stripe update, merged to main: per-plan fair-use allowances (#59, mechanism only — `FAIR_USE_MODE` still `warn`), customer usage shown as a % with no dollars exposed (#60), auth-aware site nav (#61), backend no-duplicate-Dock-icon (#62), three-dot opens Settings (#63), Settings split into User + Console with native-mic Permissions + Connectors (#64). PR #57 (desktop sync app side) confirmed merged. Operationally: transactional email now LIVE (Resend domain verified, license-key email sends, Supabase custom SMTP for confirm/reset), www `/auth/callback` redirect added, `NEXT_PUBLIC_SUPABASE_URL` suffix fixed, account creation verified end-to-end; test-trial data cleaned up; duplicate-Dock-icon diagnosed as stale DMG volumes (not a code bug), stale `com.jarvis.*` LaunchAgents removed. OPEN: #65 (open-Notes launches app, backend) and #66 (device-settings Phase 1, needs `migration_device_settings.sql`). Logged six decisions, rewrote open loops + risks (top shared-key risk now ADDRESSABLE via #59 but not yet enabled; new mic-detection + unsigned-build risks; email risk cleared), corrected the earlier "fair-use already bounded" note.
- **2026-06-11:** kuba-vault refresh — Stripe went LIVE (new "Twin Peaks Labs" account, live keys/webhook/prices, `cs_live_` verified for Pro + Ultra, a real trial purchase issued a key; payouts paused pending bank account), and a full self-service account layer shipped on valet-voice.com: Supabase-Auth email/password login + `/account` section (PR #52), show-password toggle + confirm field then eye-icon-in-field (PRs #53, #55), owner-only read-only `/account/admin` subscriber view (PR #54), and the web side of a desktop→web sync — `account_sync` table, authed sanitized `/api/proxy/sync` ingest, dashboard Profile/Speech-activity/Connected-apps sections (PR #56). Accounts + sync migrations run in prod Supabase. Desktop sync app side (PR #57: `server.py` wires `SuccessTracker` + a telemetry-gated 15-min snapshot push) is OPEN, not merged. Updated tech stack/integrations (Supabase Auth + `account_sync`, Stripe live, Resend planned), logged five decisions, refreshed open loops (bank account, transactional email, www redirect, merge #57) and risks (payouts paused, email not wired, recovery now via accounts). Closed the old "no license-key recovery" gap.
- **2026-06-11:** Added a "Roadmap / Deferred" section capturing intentionally-deferred work so it survives across sessions — next-build riders (onboarding-on-every-install #44), license-key email awaiting Resend config (#46), operator-owned config (Stripe live, Resend), not-started features (Google/Gmail, accounts/billing portal, "sir" persona pass, TTS fallback, Accessibility-API universal control, Langfuse dashboard). Noted shared-key spend is now BOUNDED (`FAIR_USE_MODE=throttle` + `FAIR_USE_MONTHLY_USD=10`).
- **2026-06-11:** kuba-vault refresh — Stage F is DONE: signed + notarized macOS DMG live as a `Kuba-Ventures/valet-downloads` `v0.1.0` release (Developer ID, Team QZX7VBLDZT, `valet-notary`), Vercel `DOWNLOAD_URL` wired; full buy → key → download → clean-install → run loop verified. Logged the onboarding wizard (PR #41) re-running per build and per fresh install (PR #44), PyInstaller `--clean` build fix (PR #42), reliable relaunch after a permission toggle (PR #43), privacy-respecting Langfuse action analytics (PRs #39/#40), and the 5-page landing + /privacy + /terms (PR #38). Phase → launch prep. Cleared the now-false "no signed build" risk; sharpened the open liabilities to: shared-key soft cap (top), Stripe still sandbox, no license-key recovery, deferred Google/Gmail, single-vendor TTS.
- **2026-06-11:** Onboarding now re-runs on every fresh install via the `.app` creation-time stamp (PR #44).
- **2026-06-11:** Reliable restart after a macOS permission toggle — watchdog polls every 0.5s, frozen backend waits for `:8340` to free before binding (PR #43).
- **2026-06-11:** `build-macos.sh` passes PyInstaller `--clean` so builds never bundle stale cache (PR #42).
- **2026-06-11:** Comprehensive first-run onboarding wizard — welcome, license, permissions, voice, profile, connections (PR #41).
- **2026-06-11:** Privacy-respecting action analytics into Langfuse (tags + metadata, no raw payloads), action tags split into type + `app:Name` (PRs #39, #40); new /privacy + /terms pages.
- **2026-06-11:** Cinematic 5-page landing overhaul — home, /how-it-works, /pricing, /faq, /contact (PR #38).
- **2026-06-10:** kuba-vault full rewrite — VALET (Vee) rename across PROJECT.md, zero JARVIS references. Captured the Phase 2 re-scaffold: Stage A proxy spine (live, `product-site/`), Stage B app repoint + license gate (`licensing.py`, 7-day grace), Stage B2 module extraction (`task_manager`/`voice_text`/`project_scanner`), Stage C portable `ActionExecutor` + AppleScript backend, Stage D risk-tiered safety (`safety.py`/`safe_executor.py`, kill switch, confirm cards), Stage E two British voices + self_mod cut from shipped builds (PR #17). Recorded integrations + costs, locked decisions, and Stage F as the only remaining work. Flagged the personal-keys / soft-cap top risk.
- **2026-06-10:** Stage E (PR #17) merged — two-voice picker (British Male/Female), `_active_voice_id()` live read; self_mod disabled + excludable in shipped builds via `_load_self_mod()`.
- **2026-06-10:** Stage D merged (PRs #15, #16) — risk-tiered safety engine (`safety.py`, `safe_executor.py`), kill switch, confirm-first Trash-safe deletes, frontend confirm card + STOP button, `/api/safety/*` routes.
- **2026-06-10:** Stage C merged (PR #12) — `action_executor.py` ABC + `Capability` enum + `applescript_executor.py` macOS backend; portable control layer.
- **2026-06-10:** JARVIS → VALET rename merged (PR #14) — persona, wake words, logger namespaces, launchd label, marketing site, README; Marvel/MCU framing retired. Local dir now `~/Code/VALET`; `jarvis-y` remote/URL preserved.
- **2026-06-10:** Stage B merged (PRs #10, #11) — app routes AI + TTS through the proxy with a license gate (`licensing.py`); in-app API-key entry removed; `task_manager.py`/`voice_text.py`/`project_scanner.py` extracted from `server.py`.
- **2026-06-10:** Stage A merged (PR #9) — license-gated AI/TTS proxy in `product-site/` (live on Vercel), Supabase `license_usage` + `record_usage()` RPC, soft fair-use metering, Langfuse tracing.
- **2026-06-10:** Supervised PR factory wired up — `factory.yml` (factory-tests + fail-closed factory-review), merge policy in `CLAUDE.md`, branch protection on main. Auto-merge OFF until a ~2-week soak.
- **2026-06-04:** Self-mod ship hardened — focus Cursor terminal by AXDescription, verify the Claude pane is live before paste (else file-ship), `abandon_feature_branch()` + scrap-branch path.
- **2026-06-03:** Render-only multi-day weather panel; ship-phrase fix; repo-rename cleanup.
- **2026-05-31:** Fixed self-mod silently dropping a ~44KB paste — `project_context.py` hard-caps warm context at 6000 chars.
- **2026-05-22:** kuba-vault initial PROJECT.md superdoc — scanned repo, reconciled README/CLAUDE.md, summarized chunks 0-33.
- **2026-05-22:** Native `[ACTION:CHECK_WEATHER]` via Open-Meteo, floating weather card.
- **2026-05-21:** Greenfield projects (design + scaffold + ship); `_looks_like_app()` routing fix; strip em-dashes at runtime.
- **2026-05-19:** Voice → Claude Code via auto-paste (Mode 1) and dictation (Mode 2).
- **2026-05-17:** Native Opus research, streaming source-preview cards, floating result panels; baseline phases 1-5.
