<!-- JARVIS Design Session 9677c191 — untitled design
     Shipped at 2026-06-04 11:12:45 via ship_method=file
     Paste this file's body into Cursor's claude terminal to dispatch. -->

Dispatch this work to the `debugger` sub-agent via the Agent tool. Use subagent_type="debugger" and pass through the brief below.

## Task: untitled design

## Goal
Change the orb's response colour to purple across all response states (speaking, thinking, idle), replacing whatever colour is currently used in those states.

## Context
No warm context loaded. Orb visual state colours are assumed to live in frontend/src/orb.ts — implementer to confirm exact file and colour constants.

## Constraints
Must replace the existing response-state colour(s) with purple (#7C3AED or similar sensible shade) for all three states: speaking, thinking, and idle. Must not affect non-response states (e.g. listening, error).

## Acceptance criteria
Running JARVIS and triggering a response causes the orb to display purple in speaking, thinking, and idle states. Non-response states are visually unchanged.

## Files to reference
- `frontend/src/orb.ts`

## Surfaced file paths (absolute)

- `/Users/finley/Code/jarvis/frontend/src/orb.ts`
