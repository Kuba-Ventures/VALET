{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "orb-drag",
  "description": "Allow the frameless orb widget to be dragged by its top handle. The UI is served from the local backend (a remote origin to the webview), so start-dragging must be granted to that origin explicitly; nothing else is exposed.",
  "windows": ["main"],
  "local": true,
  "remote": {
    "urls": ["http://localhost:8340", "http://localhost:8340/*"]
  },
  "permissions": ["core:window:allow-start-dragging"]
}
